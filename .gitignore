# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual environments (uv)
.venv/
venv/

# Test / lint / type caches
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/

# CDK
cdk.out/
*.cdk.staging/

# Node (if any UI lands later)
node_modules/

# OS / editor
.DS_Store
*.swp
.idea/
.vscode/

# Local env files
.env
.env.*
!.env.example

# Claude Code local (machine-specific) settings — keep shared memory/skills, drop local perms
.claude/settings.local.json
deploy/_local_extract/

# Superpowers scratch (progress ledger, brainstorm mockups) — session-local, not project artifacts
.superpowers/
~$*

# Supabase CLI link artifacts (per-machine, may hold connection metadata)
supabase/.temp/
