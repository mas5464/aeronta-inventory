# Tasks

## Current Session — 2026-07-20

### 2026-07-21 — Brand locked + cloud provisioning (C2 start)
- [x] **C1 merged to main (`dfe322b`) and pushed** — local main fast-forwarded to origin first (64 remote commits), then a clean zero-conflict merge; verified on merged main: agent-spine 349/6 + pg 70/1 (real Postgres) + cedar 40/40.
- [x] **Product name locked: "Aeronta Inventory"** (spec §1 updated — supersedes the deferred-codename decision).
- [x] **Cloud resources provisioned via CLIs** (Supabase/Vercel MCP connectors were unreachable; both CLIs authenticated): new Supabase org `Aeronta` (free plan) + project `aeronta-inventory` (ref `sluoxufnqwusmtckklnv`, us-east-1), repo `supabase link`ed; Vercel project `aeronta-inventory` (`prj_WQlrbadCxnWfLQOCteebIIJENzFz`, personal scope, empty until C2's first deploy). DB password only in gitignored `deploy/_local_extract/aeronta-supabase.env`; `supabase/.temp/` gitignored. Full facts table in [supabase/README.md](supabase/README.md).
- Next: C2 proper — role-bootstrap SQL + `supabase db push` (4 migrations), seed a demo tenant, Railway containers, `apps/web` on Vercel with the Supabase auth shell; the six-item C2 pre-flight checklist lives in ROADMAP.md.

### Commercialization: deep-research → spec (approved) → C1 Supabase foundation shipped
- [x] **Deep-research report** — [docs/research/2026-07-20-trax-io-saas-commercialization-research.md](docs/research/2026-07-20-trax-io-saas-commercialization-research.md): adversarially-verified platform limits, pricing, and patterns (Vercel compute caps ruling out the ML tier there, Supabase RLS/claims-hook gotchas, Railway vs Fly, Stripe billing patterns) grounding every decision cited in the architecture spec below.
- [x] **Commercialization architecture spec (approved)** — [docs/superpowers/specs/2026-07-20-commercialization-architecture-design.md](docs/superpowers/specs/2026-07-20-commercialization-architecture-design.md): "wrap and persist" — keep the tested Python backend (~1,100 passing tests) nearly intact, commercialize around it on Vercel (marketing site + `apps/web` static) + Railway (FastAPI BFF + engine worker) + Supabase (auth/Postgres/storage) + Stripe. Decomposed into C1 (Supabase foundation) → C2 (cloud deploy) → C3 (upload intake) → C4 (billing + marketing site); C1 first as riskiest and most load-bearing — everything sits on the tenancy schema.
- [x] **C1 plan** — [docs/superpowers/plans/2026-07-20-c1-supabase-foundation.md](docs/superpowers/plans/2026-07-20-c1-supabase-foundation.md) (14 TDD tasks).
- [x] **C1 executed subagent-driven** (14 tasks, fresh implementer + adversarial reviewer per task, 6 fix rounds; commits `d49b3e0`..`8d6884a`): `supabase/migrations/` (4 migrations) — tenants/memberships + `current_tenant_id()`; claims hook (`custom_access_token_hook` + defensive `try_uuid`); planner lifecycle tables (recommendations/decisions/writeback_ledger/kill_switches — decisions+ledger append-only at both grant and policy layers); seeded-view/scenario tables (part_keys/part_contexts/tenant_snapshots/scenarios/scenario_audit/bvr_cache). Every tenant-scoped table RLS'd + two-tenant isolation tested. New `services/agent-spine/src/trax_io_spine/pg/` package (`--extra pg`, psycopg3 + testcontainers): `db.py` (pool, tenant-scoped transactions, migration runner), `writeback.py` (`PgWritebackTarget`, in-memory-conformant ledger), `seed.py` (`seed_store`/`seed_tenant` + `trax-io-pg-seed` CLI, single-transaction replace semantics), `store.py` (`PgPlannerStore` — full `PlannerStore` surface: queue reads with sort/filter parity, decisions with FOR SHARE kill-switch lock, seeded-view reads, scenarios, BVR with `bvr_cache`). `bff/asgi.py` gains `DATABASE_URL` boot mode (highest precedence over `PLANNER_SNAPSHOT_DIR`).
- [x] **Notable review-driven hardenings**: defensive UUID casts in the claims hook (malformed-input tests); both-verb append-only coverage on decisions/ledger; in-transaction FOR SHARE kill-switch re-check (closes a TOCTOU); DB-side 6-table seed verification.
- [x] **Documented deviations**: (a) `approve()` commits status+decision BEFORE the writeback call — no 2PC; ledger idempotency makes retries safe; `rollback()` shares the same accepted no-2PC pattern in the opposite order (ledger write, then decision row) — a crash between them leaves a ledger entry without its decision row; (b) `dashboard()`'s `open_recommendations`/`net_cost_impact` are **entry-existence-keyed** (invariant across every decision verb this store supports), not the plan's original `status='pending'` live-merge guess — proven a mismatch against the repo's own `extract_sample` fixture (drops dedup, excludes non-pending entries the in-memory formula still counts, can't reproduce first-inserted-wins without an ordinal column), so the Pg `dashboard()` is a pure static-snapshot read (see plan Task 11); (c) tenancy RLS policies target `trax_app` only — `authenticated`-role grants arrive with C2.
- [x] **Test counts**: pg suite **70 passed / 1 skipped** (env-gated scale gate); whole agent-spine suite **365 passed / 2 skipped**; ruff clean apart from 2 pre-existing B905 in `tests/bff/test_csv_export.py`.
- [x] **Carry-forwards**: the 59K scale gate is implemented but **not yet run** (full-network snapshot unavailable — regenerate via `trax-io-precompute` from a real extract, then `PG_BENCH_SNAPSHOT_DIR=… pytest tests/pg/test_pg_scale.py`); `DATABASE_URL` boot requires a bypassrls-capable role until a security-definer slug-resolve lands (C2); Stripe/JWT-middleware/upload = C2–C4.
- Next: C2 (cloud deploy — Railway + Vercel + auth shell), or run the deferred 59K scale gate once a full-network snapshot is regenerated.

**Status: C1 Supabase foundation shipped; ready for C2 cloud deploy.**

---

## Prior Session — 2026-07-13

### Docker Deployment + Roadmap Expansion
- [x] **Deployed Trax IO v1 to Docker** — full-stack local dev environment now running:
  - Generated precomputed snapshot (`trax-io-precompute`) with 6 recommendations + feature-store data
  - Built and deployed BFF + React UI via `docker compose up --build`
  - UI running at http://localhost:8089 (single frontend, all feature-parity waves complete)
  - BFF API running at http://localhost:8001 (debug access)
  - Both services healthy and verified operational

- [x] **Expanded ROADMAP.md with v2–v6 future phases** — added comprehensive breakdown of post-v1 product evolution:
  - **v2: Causal Demand Forecasting** (forward flight plans, federated peer-benchmark, commercial SKU "Trax IO Causal")
  - **v3: AOG & Shortage Risk** (shortage prediction, expedites/transfers, commercial SKU "Trax IO AOG Shield")
  - **v4: Excess & Obsolete Recovery** (slow-movers, shelf-life alerts, first Code Interpreter use, commercial SKU "Trax IO Recovery")
  - **v5: Repair-vs-Buy Sourcing** (PO/RO/interchange/rental/loan optimization, commercial SKU "Trax IO Sourcing")
  - **v6: Rotable Pool (METRIC)** (multi-echelon optimization, discrete-event simulator, commercial SKU "Trax IO Network" — premium tier)
  - Documented per-phase features, dependencies, and pre-work blockers (OCC integration scoping for v2, AOG cost-model calibration for v3, METRIC research for v6)
  - Added v1 completion status table + cross-phase constraints table

- [x] **Documented current deployment state** in ROADMAP.md:
  - All 10 v1 sub-projects complete and verified (266 agent-spine tests + 282 Vitest + 65 Java smoke tests)
  - v2–v6 designed but not built; ready to start once pre-work gates close
  - Next immediate action: **Wave 3 #10 Tenant Onboarding Runbook** (operationalize lighthouse customer shadow→production transition)

**Status: v1 fully deployed and documented; roadmap expanded to cover the 6-phase product evolution through multi-echelon rotable optimization.**

---

## Prior Session — 2026-07-07

### Completed 2026-07-07 — Live end-to-end UAT: writeback-java against the real local eMRO
- [x] **Trigger**: user asked for an end-to-end UAT hitting the live local eMRO Oracle (`jdbc:...@localhost:1521/LOCAL`, connect-only — `oracle19c` container never touched) to create a requisition, upload levels, and create a transfer order through the real service.
- [x] **Setup**: packaged the Quarkus service and ran it on :8090 against the real DB with a UAT-minted RSA JWT (dev issuer) and a disposable Redpanda for the Kafka channels (own container, removed at teardown). Flyway needed `baseline-on-migrate=true` + `baseline-version=0` for the non-empty eMRO schema.
- [x] **All three domains ACCEPTED with real eMRO-minted references**: stock-level upsert on `E-ROT@JFK` (3/0/5/10 → 4/2/5/12, then **restored via the API** — history chain v1 → v4 with the designed version gaps, parent_version=3); requisition **766211** (`REQUISITION_HEADER/_DETAIL` verified in-DB); transfer order **TS 51124505 JFK→MEX OPEN** (`ORDER_HEADER` + `ORDER_DETAIL` E-ROT qty 1 batch 1001 verified in-DB). `WRITEBACK_LEDGER` shows the full 4-row chain with `DOMAIN`/`CREATED_REF`/`MESSAGE` populated; history + run-results read endpoints verified live.
- [x] **Production finding fixed (commit `65876b6`, rides PR #6)**: ledger reads failed with **ORA-18716** on real Oracle 19c + ojdbc 23 — Hibernate's default `Instant` mapping extracts via `OffsetDateTime`, which that driver/DB combo can't convert (JDBC probe: `getTimestamp`/`LocalDateTime` fine, `OffsetDateTime` fails). Fixed with `@JdbcTypeCode(SqlTypes.TIMESTAMP)` on `WritebackLedger.createdAt` + documented `-Duser.timezone=UTC`; README gains a "Running against a real eMRO" section (baseline config, timezone flag, `WRITEBACK_EMRO_COMPANY`). Full suite still **147/147** with the change.
- [x] **Teardown**: service stopped, UAT Redpanda removed, `E-ROT@JFK` levels restored to their original values through the API itself (an extra rollback-path proof, not a manual SQL fixup).
- Next: user merges [PR #6](https://github.com/mas5464/trax-io-inventory-optimizer/pull/6) (company override + smoke invocation + this UAT fix); production deployment items in ROADMAP #6 stay user-gated.

### Completed 2026-07-07 — eMRO Write-Back Service (Java) slice 2 — sub-project #6 real-eMRO track
- [x] **Trigger**: slice 1 ([ADR-0015](docs/adr/2026-07-07-0015-emro-writeback-java-service.md)) deferred rollback, the audit-supplement history endpoint, requisitions, transfers, replay tooling, and three carry-forwards (Kafka infra-retry unreachable, audit-PK same-second collision risk, dead `WRITEBACK_LEDGER.MESSAGE`). User's scope decision: *everything remaining*, one slice, one stacked branch (`claude/emro-writeback-slice-2` on slice 1's `ab98590`). [spec](docs/superpowers/specs/2026-07-07-emro-writeback-java-slice2-design.md) (D9–D17) → [plan](docs/superpowers/plans/2026-07-07-emro-writeback-java-slice2.md) (12 tasks) → [ADR-0016](docs/adr/2026-07-07-0016-emro-writeback-slice2.md).
- [x] **12 plan tasks executed subagent-driven, each with per-task adversarial review**: ledger `DOMAIN`/`CREATED_REF`/`MESSAGE` extension (D10); rollback (D12, contract-conforming to `fake_emro`/`RestWritebackClient` test-for-test); out-of-band history endpoint (D13); ARMAC entity lift for requisitions + transfers (byte-for-byte fidelity); `RequisitionCreator` + `TransferCreator` (validated/ledgered/effectively-once, same shape as slice 1's `StockLevelWriter`); batch + Trax IO facades for both new domains sharing one extracted `WireSanitizer`; Kafka `domain` discriminator (D14); exception taxonomy (D15) making the Kafka infra-retry path reachable; audit-PK collision mitigation (D17); the `GET /api/v1/runs/{runId}/results` replay endpoint (D16); this bookkeeping task (smoke-test extension + review-carryover ordering test + trackers).
- [x] **144 tests green** (was 143 at end of T11; +1 this task's own run-results ordering-pin test), head commit `ffc938d` before this task's docs commit.
- [x] **Notable catches during adversarial review** (all fixed before moving on, none left as silent debt): requisitions' `AUTHORIZATION`/`AUTHORIZED_BY`/`AUTHORIZED_DATE` + `UOM`-with-`EA`-fallback fields, missed by an over-broad "out of scope" assumption on the first pass (T5, commit `0bd50f3`); transfers' order/batch numeric types drifted to non-native types and were restored to ARMAC's real `long`/`BigDecimal` per the precedent requisitions' own entities had already set (T7, commit `1f77857`, "T5 precedent"); a shared `WireSanitizer` extracted after the requisitions facade's ERROR-wire sanitization repeated slice-1's per-facade pattern instead of reusing it (T6, commit `556f0da`); the Kafka domain discriminator's three near-identical per-domain pipelines collapsed into one generic route/`DomainProcessor` dispatch (T9); the `InfrastructureException` taxonomy (D15) finally closing slice 1's own carried-forward "Kafka infra-retry unreachable" finding, verified via a toggleable failure hook proving fail-fast → 3-attempt retry → DLQ is now a real path, not a folded-into-`ERROR` dead end (T10). (Slice-1's own PR #4 review round — tenant-scoped composite idempotency, `PESSIMISTIC_WRITE` old-values capture, runId-keyed Kafka results/DLQ — landed on the stacked base this branch builds on, not in this slice's own tasks.)
- [x] **Documented deviations + carry-forwards** recorded in [ADR-0016](docs/adr/2026-07-07-0016-emro-writeback-slice2.md): `parent_version` chaining diverges from `fake_emro` when a shadow write intervenes (pre-existing slice-1 behavior, unpinned by Python tests); run-results is a bare JSON array, not the brief's assumed `{runId, results}` envelope; transfers omit ARMAC's `QAR` currency hardcode, `orderOption1..10` remarks fields, and hour/minute `needBy` precision (all undocumented Trax IO inputs, not oversights); D17's `ORA-00001` message-text assumption is unverified against real Oracle, needing a production log sample.
- [x] **Deferred list** (full detail in [ROADMAP.md](ROADMAP.md) sub-project #6): production IdP/broker/deployment; a live smoke-test run against `oracle19c` (this task's smoke extension adds requisition/order table + `PKG_APPLICATION_FUNCTION` existence checks, still opt-in/env-gated); `NotePad`/remarks fields and physical stock-move side effects (documented out of scope); a PRD Phase-4 load test against NFR numbers.
- Next: a whole-branch review over everything landed since slice 1's `ab98590`, then a stacked PR onto PR #4.

### Completed 2026-07-07 — eMRO Write-Back Service (Java) slice 1 — sub-project #6 real-eMRO track
- [x] **Trigger**: sub-project #6's local hardening slice ([ADR-0010](docs/adr/2026-06-28-0010-audited-writeback-seam.md)) proved the writeback seam entirely against `fake_emro`; this slice builds the real half — a Quarkus 3 / Java 21 service that actually writes `PN_INVENTORY_LEVEL` in eMRO's Oracle DB. [spec](docs/superpowers/specs/2026-07-06-emro-writeback-java-slice1-design.md) → [plan](docs/superpowers/plans/2026-07-06-emro-writeback-java-slice1.md) (13 tasks) → [ADR-0015](docs/adr/2026-07-07-0015-emro-writeback-java-service.md).
- [x] **13 plan tasks executed subagent-driven, each with per-task adversarial review**: module scaffold + Oracle/Kafka Dev Services health check; entity/repo lift from the ARMAC reference (byte-for-byte fidelity verified); domain core `StockLevelWriter` (validation, per-item `REQUIRES_NEW` upsert, audit insert, service-owned `WRITEBACK_LEDGER` row for effectively-once delivery, bounded version-conflict retry); the `WRITEBACK_LEDGER` Flyway `V1` migration; Facade 2 (PRD batch, camelCase) and Facade 1 (Trax IO #6 seam, snake_case, wire-conforming to `services/agent-spine`'s `RestWritebackClient`/`fake_emro`) covering apply + history; Kafka in/results/DLQ; Micrometer metrics; an env-gated `oracle19c` connect-only smoke test; this bookkeeping task.
- [x] **65 tests green**, head commit `73a9cfd`.
- [x] **Notable catches during adversarial review** (all fixed before moving on, none left as silent debt): constraint-specific dedup classification + a `UNIQUE (tenant,pn,location,version)` chain (Task 6, replacing a generic-exception approach); a `PROVENANCE_ID` schema gap discovered and fixed by amending `V1` in place (Task 9); an `items:null` NPE that would have broken the always-200 batch contract (Task 7); a `JUnit AssertionError` bypassing the smoke test's restore-on-failure path, fixed by widening the catch to `Throwable` (Task 12, caught pre-live); the tier wire-format discovery that `AutonomyTier` is a Python `IntEnum` (int on the wire, not the plan's assumed string) — `TierMapper` now accepts both (Task 8).
- [x] **Documented deviations + carry-forwards** recorded in [ADR-0015](docs/adr/2026-07-07-0015-emro-writeback-java-service.md): no `409 deferred_open_order` in slice 1; `agent_version` self-identifies as `emro-writeback-java/1.0`; history served ledger-only (spec §4.1 amended); Kafka infra-retry currently unreachable pending an exception-taxonomy fix; audit-PK second-precision collision risk on real Oracle.
- [x] **Deferred list** (full detail in [ROADMAP.md](ROADMAP.md) sub-project #6): rollback endpoint, audit-supplement history, requisitions, transfers, replay tooling — all slice 2; production IdP/broker/deploy and a live smoke-test run against `oracle19c`.
- Next: slice 2 (rollback + requisitions + transfers), or a whole-branch review of everything landed since the last checkpoint.

## Prior Session — 2026-07-06

### Completed 2026-07-06 — Full-feature deck + Phases 2–6 roadmap breakdown (restored 2026-07-07)
- [x] **Full-feature PowerPoint** — [docs/decks/2026-07-06-trax-io-full-feature-overview.pptx](docs/decks/2026-07-06-trax-io-full-feature-overview.pptx) (+ PDF companion): a 27-slide dark aviation-tech deck in the real product brand palette (tokens lifted verbatim from `apps/web/src/styles/globals.css`), balanced business + technical. Native-drawn diagrams (agent orchestration, data-flow, ingestion lanes, 4-layer isolation, milestone timeline); content mined from the design doc / roadmap / full-feature guide / steering one-pager; slide 23 keeps the "what runs today vs. no-AWS-deploy-yet" honesty. Built via `pptxgenjs`; visual QA by 3 fresh-eyes subagents + a text/placeholder scan (5 layout defects found & fixed, re-verified). Build scripts live in the session scratchpad, not the repo.
- [x] **ROADMAP.md — Phases 2–6 expanded** from a terse skeleton into a full per-phase feature breakdown: an overview table (SKU / specialist / depends-on / headline value), buildable feature checklists per phase (v2 Causal → v6 Network), a cross-phase-foundations note, a **pre-work-gates** table, and renumbered **Future ADRs**. Grounded in design [§8](docs/design/2026-04-14-trax-io-inventory-optimizer-design.md)/§9; surfaced the grounded **repair-TAT data gap** (no source in the extract registry today — #12 Wave A) as the explicit v5/v6 blocker.
- [x] **Restoration note (2026-07-07)**: these two tracker edits were left uncommitted during the writeback build and were lost from the working tree mid-execution (likely a subagent tree-clean); restored from the session record with future-ADR numbers shifted to 0016–0020 since [ADR-0015](docs/adr/2026-07-07-0015-emro-writeback-java-service.md) is now taken. The deck files themselves (untracked binaries) were never lost. Lesson recorded: commit tracker/doc edits immediately; never leave session work uncommitted while implementer subagents run.

### Completed 2026-07-06 — Retired `apps/planner-ui`: single-frontend consolidation
- [x] **Trigger**: `apps/web` reached full feature parity with `apps/planner-ui` across all 4 parity waves (CSV export, writeback history + rollback, Reports/BVR view, dark/light theme — see the entries below) — the sole remaining mechanical follow-up flagged at the end of Wave 4. [spec](docs/superpowers/specs/2026-07-06-retire-planner-ui-design.md) → [plan](docs/superpowers/plans/2026-07-06-retire-planner-ui.md) (8 tasks) → [ADR-0014](docs/adr/2026-07-06-0014-retire-planner-ui-frontend.md), which supersedes [ADR-0012](docs/adr/2026-06-28-0012-planner-ui-react-frontend.md).
- [x] **Deleted**: the `apps/planner-ui` app (source + `node_modules`-adjacent config + `UAT.md`), its Dockerfile, and its `docker-compose.yml` `ui` service + dev launch configs; removed the retired frontend's UI-slice specs/plans (visual-redesign phases, drawer/bulk/contrast, ConfidenceHero refinement, etc. — git retains their full history).
- [x] **Reframed surviving docs + scrubbed incidental mentions**: docs that describe the still-live BFF/backend were reframed to stop describing "two frontends," while explicitly preserving the BFF's "Planner-UI BFF" / `PLANNER_*` identity (the backend keeps its name — only the retired React app's path references were scrubbed from comments/docstrings/test names).
- [x] **`CLAUDE.md` rewritten**: `apps/web` is now documented as the sole frontend; the historical `apps/planner-ui` narrative arc is removed from the project's own instructions (git retains it).
- [x] **This file + `ROADMAP.md` updated**: current-status sections now describe the single-frontend state; granular `apps/planner-ui` phase/slice log lines dropped (git retains them); this entry is the dated retirement record for both trackers.
- Next: Wave 3 **#10 tenant-onboarding runbook**, the **AWS halves of Waves 0–1**, or **forecasting slice D** (needs real SageMaker) — same three-way fork as before.

## Prior Session — 2026-07-02

### Completed 2026-07-06 — apps/web dark/light theme (feature-parity Wave 4 of 4, FINAL)
- [x] **Wave 4 of the parity effort — the last one**: brought a dark/light theme toggle to `apps/web`, closing the 4-gap parity effort found in Wave 1's analysis (CSV export, writeback history + rollback, Reports/BVR view, dark/light theme). [spec](docs/superpowers/specs/2026-07-06-apps-web-dark-light-theme-design.md) → [plan](docs/superpowers/plans/2026-07-06-apps-web-dark-light-theme.md) (2 TDD tasks) → subagent-driven development.
- [x] **Task 1 — `useTheme` hook**: `src/lib/useTheme.ts`, dark-first and user-toggleable, backed by `localStorage` key `trax-web-theme`. `:root` is dark by default and a `.light` class on `<html>` opts into the light override, so `applyTheme()` toggles the class rather than setting a `data-theme` attribute. Any non-`"light"` stored value (including absent/garbage) resolves to dark. Returns `{ theme, toggleTheme }`.
- [x] **Task 2 — header toggle + pre-paint anti-flash script**: `App.tsx`'s header gains a `flex items-center justify-between` row with the existing title plus a new sun/moon `<button>` (`lucide-react` `Sun`/`Moon`) calling `toggleTheme()`; `aria-label` names the destination theme, not the current one. `index.html`'s `<head>` gains a small inline `<script>` (after `<title>`, before the `/src/main.tsx` module script) that reads `trax-web-theme` from `localStorage` and adds `.light` synchronously before the bundle loads — since `:root` is already dark, only the light case needs a pre-paint override, avoiding a dark→light flash for returning light-mode users. TDD: extended `App.test.tsx`'s `afterEach` to also reset `localStorage`/`document.documentElement.className`, wrote the new toggle test first, confirmed exactly 1 new failure (no toggle button yet) against 6 pre-existing passes, then implemented — all 7 App tests green after.
- [x] **288 Vitest tests (was 282) + build/lint clean.** Live-verified end-to-end on the rebuilt Docker `web` (:8089, real 58.9K-key network data): clicking the header toggle flips the whole app — background, cards, nav, icon — between dark and light on both Overview and Reports; `localStorage` correctly persists `"light"` across a hash-route navigation and a full page reload (deep-linked back to `#/reports` still light); zero application console errors (only generic Chrome-extension messaging noise, unrelated to the app).
- [x] **All 4 feature-parity waves are now complete.**

### Completed 2026-07-06 — apps/web Reports/BVR view (feature-parity Wave 3 of 4)
- [x] **Wave 3 of the parity effort**: brought the #8 Business Value Report to `apps/web` as a dedicated `Reports` view, over the EXISTING BFF `GET /v1/tenants/{tenant}/reports/bvr{,.html,.pdf}` routes (no backend change). [spec](docs/superpowers/specs/2026-07-06-apps-web-reports-bvr-design.md) → [plan](docs/superpowers/plans/2026-07-06-apps-web-reports-bvr.md) (5 TDD tasks) → subagent-driven development.
- [x] **Data + hooks**: `BvrReport`/`BvrSavings` TS types (field-perfect vs the pydantic contract) + `getBvr`/`bvrDocumentUrl` client methods + `useBvr` hook (`staleTime: 60_000`, matching `useDashboard`/`useFeeds`); pure `reportView` helpers (`savingsComponentLabel()` maps the raw `snake_case` deltas to human labels — "Holding cost"/"Ordering cost"/"Stockout risk" — never the raw key; `formatRatePct`/`formatAmount`).
- [x] **UI**: a new `Reports` view rendered as a document (hero tiles — total projected, changes applied/shadowed, keys under management, open pipeline, tier posture; savings-components breakdown; governance strip — approval/override rates, kill-switch state; forward-look section with `/parts/:pn/:location` links into the existing Part Drill-Down; methodology section disclosing "N of M portfolio keys" valued + formula strings) — deliberately **outside** the `Metric`/`ProvChip` provenance invariant used by every other view (verified by grep: 0 code hits), since a BVR is itself already a governed report, not a live operational number; wired into `App.tsx` as a live `/reports` nav item (last in `NAV_ITEMS`, after "Data & Connections") + a `<Route path="/reports">`.
- [x] **Subagent-driven**: 5 tasks, each implementer + adversarial reviewer. Tasks 2/3/4 zero-finding; Task 1 one Important fix applied during review (`getBvr` describe block used `vi.restoreAllMocks()`, which doesn't undo `vi.stubGlobal()` — corrected to `vi.unstubAllGlobals()` to match the file's own convention) plus a cosmetic Minor. Task 5 (the router wiring) followed strict TDD: extended nav-label test + a new deep-link test written first, confirmed exactly 2 failures / 4 pre-existing passes, then the 3-line `App.tsx` wiring landed and all 6 App tests passed.
- [x] **282 Vitest tests + build/lint clean.** Live-verified end-to-end at network scale on the rebuilt Docker `web` (:8089) via a real browser click-through: clicking "Reports" moves `aria-current="page"` onto it and off Overview; hero reads "57,605 Keys under management" / "$419170109.29 Open pipeline" / "41,740 recommendations"; savings rows read "Holding cost"/"Ordering cost"/"Stockout risk" (never `snake_case`); governance reads "approval rate 0.0% · override rate 100.0%"; methodology reads "Valued 57,605 of 58,899 portfolio keys"; a forward-look `ACD18990-1`/`FLL-B10` link lands on that part's real Part Drill-Down (full real data, no error state); "Open printable report" (`bvr.html`) and "Download PDF" (`bvr.pdf`) hrefs independently confirmed resolving `200` (`text/html` 10.4KB, `application/pdf` 27.4KB) through the same nginx BFF proxy. Zero console errors across the whole session; every network request `200`.
- Next: Wave 4 (dark/light theme, now complete — see the entry above).

### Completed 2026-07-06 — apps/web writeback history + rollback (feature-parity Wave 2 of 4)
- [x] **Wave 2 of the parity effort**: brought a writeback-history timeline + rollback to `apps/web`, over the EXISTING BFF `history`/`rollback` routes (no backend change). [spec](docs/superpowers/specs/2026-07-06-apps-web-writeback-history-rollback-design.md) → [plan](docs/superpowers/plans/2026-07-06-apps-web-writeback-history-rollback.md) (6 TDD tasks) → subagent-driven development.
- [x] **Data + hooks**: TS types (`HistoryEntry`/`RollbackRequest`/`RollbackResult` + `WritebackStatus`/`RollbackStatus`, verified field-perfect vs the pydantic contracts incl. the int-`tier` trap) + `getHistory`/`rollback` client methods + `useHistory` (enabled-guarded) / `useRollback` (invalidates only `["history",tenant]`).
- [x] **UI**: `WritebackHistory` timeline section on Part Drill-Down (newest-first `<ol>`, color-coded+text `Badge` status, `ROP·EOQ·SS·Max` value summary — audit events, deliberately no `ProvChip`; pure `writebackView` helpers) gated by `latestRevertibleEntry` (the rule: the latest `written` entry with non-null `old_values`) → a `useFocusTrap` `RollbackConfirmDialog` (from→to values, required reason, `principal:"planner"`); `#history` deep-link (`scrollIntoView`) reachable from a per-row Workbench "History" link.
- [x] **Subagent-driven**: 6 tasks, each implementer + adversarial reviewer. Tasks 1/2/5 zero-finding; Tasks 3/4/6 Minor-only (formatter overlap, untrimmed-reason-on-submit, dup className — all judged acceptable by the opus final review). One real catch mid-slice: the Task 3 implementer found + fixed a genuine render-timing race in the brief's own test (reviewer reproduced 10/10 pre-fix failures); Task 5 correctly upgraded the existing PartDrillDown tests' blanket fetch stub to a URL-router so the new history query wouldn't corrupt them. Opus final whole-branch review verified the TS↔pydantic contract, the revertible→rollback→refetch loop end-to-end (incl. backend appending a new `written` entry), and the provenance-no-ProvChip boundary — **"Ready to merge: Yes."**
- [x] **261 Vitest tests + build/lint clean.** Live-verified end-to-end at network scale on the rebuilt Docker `web` (:8089): approved a real rec via the BFF → its `written` entry appeared in the timeline with the correct value summary → rollback button correctly **disabled** (v1 has null `old_values`, so not revertible — the gate works live) → 25 Workbench "History" deep-links with the correct `#/parts/.../…#history` href → rollback endpoint returns `nothing_to_revert` for the non-revertible key.
- Next: Waves 3–4 (Reports/BVR view, dark/light theme), each its own cycle.

### Completed 2026-07-06 — apps/web CSV export (feature-parity Wave 1 of 4)
- [x] **Trigger**: user asked to fold the earlier, now-retired second frontend's features into `apps/web` so there's "one full feature application" (retirement now done — see the entry above). A source-grounded gap analysis found only 4 genuine gaps (CSV export, writeback history + rollback, Reports/BVR view, dark/light theme) — kill switch/bulk-approve/dashboard KPIs already existed in apps/web. Ordered fastest-first; this is Wave 1 (CSV export). [spec](docs/superpowers/specs/2026-07-06-apps-web-csv-export-design.md) → [plan](docs/superpowers/plans/2026-07-06-apps-web-csv-export.md) (6 TDD tasks) → subagent-driven development.
- [x] **Backend** (`services/agent-spine`): `PlannerStore.list_queue_all` (unpaginated sibling of `list_queue_page`, reuses `_sorted_entries`); `queue_rows_to_csv` + `CSV_COLUMNS` (stdlib `csv`, a 14-column shape); `GET /v1/tenants/{tenant}/recommendations/export.csv` route — same filter/sort params as the queue route minus limit/offset, `Content-Disposition: attachment`, **declared before `/recommendations/{rec_id}`** (load-bearing: FastAPI matches in declaration order, else `export.csv` is captured as `rec_id`; regression-guarded by a test). 150 BFF tests, ruff clean.
- [x] **Frontend** (`apps/web`): `recommendationsExportUrl()` URL builder (mirrors `getQueue`'s param shape); **Export CSV** links on Workbench (filter-aware — `href` reflects active `queryState`, `"all"`→omitted) and AI Recommendations (fixed `status=pending`, full pending queue). Real `<a href download>` via `Button asChild` — browser navigation, not `fetch()` (also sidesteps the standalone-dev CORS gap). 237 Vitest, build + lint clean.
- [x] **Exports the FULL filtered set, not a page.** Verified live end-to-end through the nginx production path (:8089, real 58.9K-key data): 200 `text/csv`, correct filename, 14-column header, 11,082 rows == paged total; `tier=1` narrows to 9,811 == paged total; Workbench `tier=2` deep-link → `&tier=2` in the href; AI Recs href fixed to `status=pending`.
- [x] **Subagent-driven**: 6 tasks, each implementer + adversarial reviewer. Tasks 3/5/6 zero-finding; Tasks 1/2/4 one Minor each (vacuous approved-status assertion over an empty fixture slice; `dict(zip)` test hardening; duplicated `sortDir` inline union) — all judged acceptable-as-is by the opus final whole-branch review, which independently verified the frontend↔backend query-param contract, the CSV-column contract vs real `QueueRow` fields, the full-set-not-page guarantee, and route ordering. **Ready to merge: Yes.**
- Next: Waves 2–4 (writeback history + rollback, Reports/BVR view, dark/light theme), each its own brainstorm/spec/plan cycle.

### Completed 2026-07-06 — Full end-to-end UAT pass (found + fixed a real bug) + two new product-documentation guides
- [x] **Trigger**: user requested a full e2e UAT pass across the whole product, conditional on a clean result, followed by two new documentation deliverables — a full feature guide and an AWS infrastructure guide.
- [x] **UAT scope**: the documented regression gate + living `UAT.md` manual-case walkthrough for both release surfaces, executed via parallel agents (one `apps/web` run stalled mid-execution on its first attempt — a confused "waiting for another process" message after only 7 tool calls — and was re-dispatched). Backend: **835 passed, 1 skipped (Oracle-gated, expected), 0 failed** across all 8 packages, `ruff check` clean everywhere.
- [x] **A stray/duplicate agent reported a false "PASS" for `apps/web`; the real retry found a genuine bug.** Two contradictory completion notifications arrived for the same `apps/web` retry dispatch — one claiming 77/80 pass with 0 fail, one (matching the actual dispatched agent ID) reporting 68/74 pass with 1 real, rigorously-evidenced fail. Rather than trust either blindly, verified live myself against the real running app: **confirmed the bug directly** — `apps/web`'s Part Drill-Down (and by extension every other view's error path) hangs on "Loading…" forever instead of showing the error/Retry UI whenever its query fails, reproduced on both the CORS-broken dev server and, decisively, on the same-origin Docker deployment with a clean, real `404 Not Found` response (no CORS involved).
- [x] **Root-caused and fixed**: instrumented the live query state directly (not guesswork) — `fetchStatus` was stuck at `"paused"` after the first failure, meaning the QueryClient's default `networkMode: 'online'` was pausing the configured `retry: 1` attempt indefinitely (its `online`/`offline` browser-event dependency never resolving in this context) and the retry that would have settled the query into a clean error state never fired. Fix: `apps/web/src/main.tsx`'s `QueryClient` now sets `retry: false` (every view already has its own manual Retry button — an automatic retry was never load-bearing) plus `networkMode: "always"` as defense-in-depth. Verified live end-to-end: error UI now renders in ~2s with a working Retry button, a valid part still loads normally, 231/231 Vitest + clean `tsc`/build/lint (this exact path was never exercised by the suite — every test file constructs its own local `QueryClient` with `retry: false`, which is exactly why 231 green tests never caught it). Rebuilt and redeployed the `apps/web` Docker container; re-verified the fix live against the rebuilt image (had to cache-bust — the browser had the pre-fix `index.html` cached).
- [x] **Corrected verdict: `apps/web` PASS-after-fix**, not the false first-pass "0 fail." 77/80 (well, 74/80 in the accurate retry's count — see full detail in that turn) live cases pass post-fix, 0 known fail, 3 not-applicable. Backend verdict stands unchanged.
- [x] **`docs/guides-src/04-full-feature-guide.md`** (+ compiled `docs/guides/Trax-IO-full-feature-guide.docx` via pandoc): documents what the v1 reference implementation actually does today — the real local/Docker system (nightly-extract → in-memory feature store → recommendation engine → agent-spine orchestration/guardrails/audited writeback → one FastAPI BFF → frontend → BVR reporting) — explicitly distinguished from the existing Architecture Guide's target AWS-native design, which predates any of this session's implementation work.
- [x] **`docs/guides-src/05-aws-infrastructure-guide.md`** (+ compiled `.docx`): grounded directly in the real CDK source (`infra/feature-store/stacks/feature_store_stack.py`, `infra/observability-soc2/observability_soc2/stack.py`), not assumed from the design doc. Both packages are real, `cdk synth`-tested (11 + 40 tests) infrastructure-as-code — per-tenant KMS/S3/Glue+Iceberg/DynamoDB for the data plane, account-wide CloudTrail Lake + Audit Manager + Object-Lock audit bucket + per-tenant KMS/log-groups for the control plane — but **zero AWS resources are deployed anywhere**; `infra/feature-store/app.py` explicitly says not to `cdk deploy` this scaffold. The compute/agent plane (Bedrock AgentCore, Strands, SageMaker) and the event plane (EventBridge, Kinesis, Lambda, Step Functions) have no code at all yet — flagged plainly rather than implied-done, matching this project's own established honesty-about-status culture (feed connection statuses, BVR's `keys_total_portfolio` disclosure, `apps/web`'s provenance invariant).
- **Lesson captured**: a background-agent completion notification's task-id not matching any ID this session actually dispatched is a real signal to distrust it, not a bookkeeping detail to shrug off — the false "PASS" here would have shipped a real bug into the historical record if taken at face value. When two reports disagree, verify live against the real app rather than picking the more convenient one.
- Next: the same three-way fork as before (Wave 3 #10 tenant-onboarding runbook / AWS deploy pipeline — Phase 8, per the AWS Infrastructure Guide's own gap list / forecasting slice D needing SageMaker) remain.

### Completed 2026-07-04 — agent-spine: guardrail-reason humanization + BVR/DemandTrend readiness fixes (5 commits, not a numbered phase)
- [x] **Trigger**: surfaced while assessing customer-testing readiness — [spec](docs/superpowers/specs/2026-07-04-customer-testing-gap-remediation-design.md) → [plan](docs/superpowers/plans/2026-07-04-customer-testing-gap-remediation.md) (5 tasks, TDD) → subagent-driven development. All 5 commits reviewed clean, "Ready to merge: Yes."
- [x] **Reason-text fix**: `RecommendationDetail`/`QueueRow.reason` was being silently overwritten by raw internal guardrail codes (`non_policy_recommendation`, `delta_exceeds_100pct`, etc.) instead of the recommender's own human-authored sentence, whenever any guardrail-pipeline reason was attached. Fixed in `services/agent-spine`: new `trax_io_spine.guardrail.messages.humanize_guardrail_codes()` maps the real internal codes to human text; `bff/store.py` now always sets `reason = rec.reason`; a new `guardrail_notes: tuple[str, ...]` field on `RecommendationDetail` exposes the humanized codes separately.
- [x] **266 passed / 4 skipped agent-spine tests (was 250 passed / 4 skipped)** — ruff clean. Trackers (`CLAUDE.md`/`ROADMAP.md`/`TASKS.md`) updated.
- Next: the same three-way fork as before (Wave 3 #10 tenant-onboarding runbook / AWS-blocked waves / forecasting slice D needing SageMaker).

### Completed 2026-07-02 — BVR follow-ups: 6 reviewer-triaged items closed
- [x] **Scope**: closed 6 of the 7 optional follow-ups triaged at the #8 final review (`.superpowers/sdd/progress.md:74`) — the `-0.00` Decimal edge case already has its own background task and was left untouched. Grounded every item in the actual code (file:line) before writing a plan via `EnterPlanMode`, rather than acting on the one-line triage note alone.
- [x] **`attribution.py`** (3 items): commented `_ATTRIBUTED` explaining why `DEFERRED_OPEN_ORDER`/`FAILED` writes carry no value and mustn't inflate `changes_total`, + extended the existing FAILED-exclusion test with a `DEFERRED_OPEN_ORDER` case; `value_change()` now floors stockout coverage at 0 (`max(0.0, min(rop, ltd))`) matching the spec's explicit "floored at 0" text, + a negative-ROP regression test; `ChangeValue.ordering_skipped` → `build_savings()` tally → surfaced as `ordering_cost_delta.inputs["ordering_skipped"]` only (scoped to `inputs` per the reviewer's own wording — no schema bump, `inputs` is already an open dict).
- [x] **Keys-subset disclosure (the one real schema change)**: `Methodology.keys_total_portfolio` — the tenant's full `(pn, location)` universe vs `keys` (the KeyStats-valued subset) — mirrors `ScenarioSolver.total_keys_in_universe`'s existing pattern exactly. **`BvrReport.SCHEMA_VERSION` 1.0.0 → 1.1.0** (additive). Threaded through `build_bvr_report` (optional param, defaults to `len(key_facts)`) → `PlannerStore.bvr()` (passes the real `len(self.keys)`) → the printable HTML Methodology paragraph ("N of M portfolio keys") → the frontend TS mirror (`types.ts` + `sample.ts`). Schema-lock tripwire + semver tests updated deliberately.
- [x] **`bff/app.py` safe-degrade on report routes**: the 3 BVR routes called `_store(tenant_id).bvr()` directly with zero exception handling — unlike every other route in the file; confirmed via the existing test file that nothing exercised this gap. New `_bvr_or_500()` helper (also de-duplicates the repeated call across `bvr_json`/`bvr_html`/`bvr_pdf`) maps an unexpected report-construction failure to a clean 500 instead of an unhandled traceback; new monkeypatch test proves all 3 routes degrade cleanly. Flagged explicitly in the plan as a best-judgment interpretation of a terse, unelaborated reviewer note.
- [x] **Spec doc fix**: `2026-07-02-bvr-pipeline-v1-local-design.md` §1 — `lead_time_p50_days` → `lead_time_mean_days` (the surrounding prose says "mean," and the actual code uses `mean_per_day`/`lead_mean`, both means; doc-only, zero code risk).
- [x] **Verified**: agent-spine **250 passed, 4 skipped** (was 245/4; +5 new tests), ruff clean. Live-verified against the running full-network Docker deploy (`docker compose build bff`): `GET /reports/bvr` shows `methodology.keys_total_portfolio` ≥ `methodology.keys`; `/reports/bvr.html` renders the new "N of M portfolio keys" line.
- Next: same three-way fork as before, owner's call — Wave 3 **#10 tenant-onboarding runbook** (no infra dependency, doable now), the **AWS halves of Waves 0–1** (blocked in this environment — `aws sts get-caller-identity` returns `InvalidClientTokenId`), or **forecasting slice D** (needs real SageMaker). The `-0.00` Decimal edge case still has its own open background task.

### Completed 2026-07-02 — #8 BVR pipeline v1-local (Wave-2 exit item): first Business Value Report delivered
- [x] **Brainstorm → spec → plan → build**: [spec](docs/superpowers/specs/2026-07-02-bvr-pipeline-v1-local-design.md) → [plan](docs/superpowers/plans/2026-07-02-bvr-pipeline-v1-local.md) (9 tasks) → subagent-driven development (8 code tasks, fresh implementer + adversarial reviewer each). **Honesty contract (owner decision):** projected-only figures vs the genuine pre-agent baseline (extract `CurrentPolicy` + audited writeback ledger); realized-vs-counterfactual explicitly deferred until sequential monthly extracts exist.
- [x] **`trax_io_spine.bvr`**: schema-locked `BvrReport 1.0.0` (frozen pydantic + field-snapshot tripwire); attribution decomposition (holding 0.25/yr · ordering $85 · stockout-proxy 0.10 tier-weighted, hand-computed fixtures, applied/shadowed split, "N of M valued" coverage); assembly (tier posture vs §5.5 targets — posture-not-realized; governance incl. `rollback:`-prefix count + A/B/C tier mix verified vs design §6.1; top-10 forward look; determinism test); Jinja2 printable HTML + inline-SVG charts (`bvr` extra — Chart.js/pyppeteer dropped); WeasyPrint PDF (`pdf` extra, skip-clean).
- [x] **BFF**: `iter_history` tenant-wide ledger enumeration; memoized `PlannerStore.bvr()` invalidated by all 5 decision paths; `GET /reports/bvr{,.html,.pdf}` (501 without the pdf extra); a live Reports view (hero tiles, applied/shadowed split, governance strip, printable/PDF links; TS mirror verified field-perfect). Dockerfile: apt pango/cairo + `--extra bvr --extra pdf` (B7 review proved bff-only sync would crash boot on the jinja2 import).
- [x] **Reviews caught 2 real bugs (both plan-authored, both fixed + regression-locked):** (1) `select_autoescape(["html"])` never matches `bvr.html.j2` (final suffix `.j2`) → autoescape silently OFF → live HTML injection via tenant/PN fields; now `autoescape=True` + hostile-value test. (2) Methodology listed all ~38.7K distinct rec hashes at network scale → 2.6MB JSON/HTML, 2m01s PDF; now 12-hash sample + `input_snapshot_hash_count` → **5.4KB JSON, 27KB PDF in 2.2s**.
- [x] **First BVR live on the 58.9K deploy (measured):** JSON 1.0s; honest zeros pre-approval (savings $0.00, changes 0/0); **live loop proven** — approve 1 rec → applied $0.00 → −$0.06 (negative reported as-is), writes_written 1, cache invalidation live. 57,605 keys under management (KeyStats-derivable subset of 58,899); posture rates 24–40% by tier (honest). UAT.md section P (7 cases). **242 agent-spine (incl. 30 bvr/reports) tests**, ruff clean.
- **Env lesson recorded** ([lessons](.claude/memory/lessons.md)): repo-path space → venv console scripts are `/bin/sh` exec-wrappers → macOS SIP strips `DYLD_*` → `python -m pytest` required for the WeasyPrint test locally.
- **Final review + merge done same day**: fable whole-slice review ('With fixes') → all 4 Important findings fixed in `30383e6` (kill-switch cache invalidation, exact applied+shadowed identity, UAT gate command, PDF-501 surfacing + Content-Disposition + money signs + full schema-lock) → live re-verified → **merged to main `239eb26`**. Wave-2 exit items are locally complete.
- **Next session**: pick the frontier — Wave 3 **#10 tenant-onboarding runbook**, the **AWS halves of Waves 0–1** (CloudTrail Lake/Audit Manager deploy, Glue jobs on a lighthouse extract, real eMRO writeback REST), or **forecasting slice D** (SageMaker + champion/challenger + 45-day gate). Optional small follow-ups logged in `.superpowers/sdd/progress.md` (DEFERRED_OPEN_ORDER test, keys-subset disclosure in Methodology, stockout floor wording, spec §1 mean-vs-p50). Local Docker stack (trax-io-planner: :8001/:8088/:8089) left running with the BVR live.

### Completed 2026-07-02 — Drill-panel search (the F1 primitive's unused searchAccessor, wired)
- [x] **Grounded first**: `useTableState`'s `searchAccessor` had zero callers; live row counts said the only table that earns a search box is the Overview drill `BreakdownTable` (by-ATA = 48 rows; tier/criticality/part-class are 3–5; feeds 13, SL bands 5, top-shortages 10). User approved: BreakdownTable only, gated.
- [x] **TDD**: search input renders only when the unfiltered breakdown has **≥ 15 rows** (`SEARCH_MIN_ROWS`); matches the **displayed** label (`labelFor` — "ATA 29"/"Tier A", not raw keys) with the active sort preserved; narrowed-to-zero shows `No <rowNoun> rows match "<query>".` instead of the misleading no-data message. Input styled + labeled like the existing `PartStatSheetLookup` pattern.
- [x] **231 Vitest tests (was 228; +3)**, build clean, lint 0 errors (2 pre-existing badge/button warnings). Web container rebuilt → :8089 serving the new bundle.
- [x] **UAT.md catch-up**: the F3 drill panels had no UAT cases at all — added **B7** (drill open/close/single-open/Escape/focus) + **B8** (drill search: threshold, displayed-label narrowing, no-match message); B section 6→8 cases; stale "142 tests" headline counts corrected to 231 everywhere. CLAUDE.md apps/web row + bullet updated (F1–F5 arc + search described, counts 231).
- Next: **#8 BVR pipeline** (Wave 2 exit) — the last unstarted Wave-2 sub-project.

### Completed 2026-07-02 — Full-network 62K run (W3-6 closed): scope fix + extract + precompute + deploy
- [x] **Recon + read-only measure**: network-wide planning-active universe = **24,241 PNs / 62,492 distinct `(PN,LOCATION)` keys** (live Oracle, matches the Wave-3 spec's April measurement exactly); prior deploy's 10K-part cap was an *alphabetical* prefix, not a sample.
- [x] **Caught a real scope bug before cutover** (the fat run was measured, diagnosed, and discarded — never deployed): the W3-5 network-wide path scoped `part_location` domains by part IN-list only ("same effective WHERE as `part`"), so **every location row** of each planning-active part landed — 2,114,517 policy rows → the pooled loader admitted **984,021 planning keys** (true: 62,492) → 762,662 recs, 2h12m precompute, 2.9GB of garbage snapshot.
- [x] **Two-layer fix** (`effa01e`): `wrap_scoped_sql` network-wide `part_location` gains a planning-active **`EXISTS` row filter** on `PN_INVENTORY_LEVEL` (same definition as `resolve_scope_planning_active`; station + `part` paths byte-identical), and the loader's **pooled branch skips zero-policy rows** entirely (no key, no `current_policy` seed; unpooled path unchanged — defense in depth). TDD both layers; **115 extract + 147 reco tests**, ruff clean.
- [x] **Corrected run**: extract 2m48s (policy pull **124,975 rows**, was 2.1M), precompute **786s** (was 7,919s on the fat universe) → **58,899 judgeable keys** (94% of the 62,492 planning-active universe — same intersection ratio as the 10K run's 22,899→21,215), **41,740 recs** (proportional to 12,876 @ 21.2K), `feature_store.json` **152MB** (final review projected ~160MB).
- [x] **Deploy cutover + measured**: compose → `emro_net_full_snapshot`; **boot 14.3s** (container start → uvicorn ready; start_period 30s ≈ 2× holds), **BFF RAM 1.515GiB** (projection band 1.6–1.9GiB), dashboard **58,899 parts / $400.1M on-hand / 71 AOG-exposed / 38,733 keys-with-a-rec** (`open_recommendations` counts keys, not recs), queue **11,082 pending + 30,658 hard-rejected = 41,740** ✓, feeds + UI verified.
- **Honest note:** portfolio KPIs shifted disproportionately vs the 21.2K deploy (on-hand units 6.9×, projected demand 11× for 2.8× keys) — the alphabetical 10K cap under-sampled high-volume parts; the full network is the first representative portfolio.
- Remaining nice-to-have from the reviewer's watch-list: nested interning of per-PN observation arrays only if RSS ever becomes the constraint (at 1.5GiB it is not).

### Completed 2026-07-02 — Fast-boot feature-store snapshot (BFF boot ~7s, was ~190s)
- [x] **Brainstorm → spec → plan → build**: [spec](docs/superpowers/specs/2026-07-02-fast-boot-feature-store-snapshot-design.md) → [plan](docs/superpowers/plans/2026-07-02-fast-boot-feature-store-snapshot.md) (5 TDD tasks) → subagent-driven development (fresh implementer + reviewer per task; all 4 code tasks **Approved with zero Critical/Important findings**). Closes the follow-up flagged by the 22.9K deploy (`49f94d9`): recs were precomputed but boot still rebuilt the fs from the 282MB extract.
- [x] **T1 `trax_io_feature_store/snapshot.py`** — format-v1 interned JSON: per bucket `values[]` (unique `model_dump(mode="json")`, deduped by `id()`) + `entries[]` (key→index); `load_store` validates each unique value once (`extra="forbid"` ⇒ schema drift fails loudly at boot) and restores instance sharing; `_BUCKET_MODELS` pinned == `InMemoryFeatureStore._BUCKETS` by test. 7 new tests (round-trip incl. Decimal, `is`-sharing, fail-loud format/bucket/validation/non-model) — **108 feature-store tests**.
- [x] **T2 precompute** — `trax-io-precompute --out` is now a **complete snapshot dir**: + `feature_store.json`, `keys.json`, `manifest.json` (copied for feeds); meta gains `snapshot_format`/`keys`.
- [x] **T3 `PlannerStore.from_snapshot_dir`** — boots from the dir with no extract parsing/pooling/engine/`InMemoryInventoryState`; fail-loud (missing artifact → `FileNotFoundError`, tenant mismatch → `ValueError`; manifest stays tolerant). **Equivalence proven vs `from_extract`** on queue rows / `dashboard()` / `part_context()` / keys / `_manifest`; ids load verbatim.
- [x] **T4 asgi** — env read inside `build_app()` (now testable); precedence `PLANNER_SNAPSHOT_DIR` → `PLANNER_RECS_FILE` → `from_extract`; test proves the fast path via ids-verbatim (a `from_extract` fallback mints fresh ULIDs and fails it). **212 agent-spine tests**, ruff clean both packages.
- [x] **T5 OPS (measured)** — regenerated the real snapshot (12,876 recs / 21,215 keys, 205s offline; `feature_store.json` **55MB** vs 282MB extract); compose flipped to `PLANNER_SNAPSHOT_DIR` (extract mount **removed**, `start_period` 240s→**30s**); redeployed. **Boot: container start 11:33:49.9 → uvicorn "startup complete" 11:33:56.97 ≈ 7s (was ~190s, ~27×)**. Live-verified: dashboard identical to pre-flip (21,215 parts / 12,065 open recs / $197.8M), queue paginating, feeds 4/3/6 off the snapshot manifest, UI 200, BFF RAM 619MiB.
- **Honest finding (corrected in final review):** top-level identity interning captured **zero sharing** on real data (115,300 unique == entries) — the pooled loader builds a FRESH `StockPosition`/`DemandHistory` (and fresh observation lists) per planning key (`extract_loader.py` pooled branch), so nothing is instance-shared in-process either. Consequence: snapshot-reload RAM is at **parity with `from_extract` by construction** (619MiB measured). Interning stays (costs ~nothing, unit-locked, pays off automatically if the loader ever shares instances); if 62K RSS becomes the constraint, the remedy is nested interning of the value-identical per-PN observation arrays or loader-level sharing — not compression. (Also: the 21,215 served keys vs the 22,899 extract-time planning-active rows is pre-existing drift, identical pre/post flip — not introduced by this slice.)
- Minor review findings logged in `.superpowers/sdd/progress.md` for the final whole-branch review (malformed-JSON `KeyError` vs `SnapshotFormatError` in `load_store`; stale `--out` help text; `FileNotFoundError` path not repr'd; no dedicated RECS_FILE-precedence test).
- Next: the **full-62K scope fix** (lift `--scope-max-parts`, re-extract, re-precompute — now lands on a ~7s-boot deploy), wiring `useTableState`'s unused search into a table, then #8 BVR pipeline (Wave 2 exit).

## Prior Session — 2026-07-01

### Completed 2026-07-01 — `apps/web` Slice S8: Hardening (final slice of the second frontend)
- [x] **a11y pass (WCAG 2.1 AA)** across all 7 views: dependency-free `useFocusTrap` hook (traps Tab/Shift+Tab, closes on Escape, restores focus on close) wired into `RejectDialog` (Workbench) and a new `CommitConfirmDialog` (extracted from `SavedScenarios`); `focus-visible` rings added to the primary nav (`App.tsx` — `NavLink` already auto-sets `aria-current="page"`, just needed the ring class); `scope="col"` added to the 2 tables missing it (Workbench, Part Drill-Down open-orders) for consistency with the 4 that already had it; verified zero icon-only buttons and zero color-only status indicators exist anywhere in the app (both already disciplined pre-slice).
- [x] **Real bug caught + fixed via TDD, not assumed**: `SavedScenarios`'s commit-confirm dialog swaps its own trigger ("Commit" button) out of the DOM in the same render that mounts the dialog — proved empirically (via a scratch probe) that `document.activeElement` is already `<body>` by the time the dialog's own effect runs, AND that React re-renders a **brand-new** "Commit" DOM node on close (not the original clicked node), so even a captured element reference can't restore focus. Fixed with a per-scenario `commitButtonRefs` map + a parent-level effect that re-focuses the freshly-rendered button after the state flips back — `useFocusTrap.ts`'s docstring now documents this pattern for future callers.
- [x] **Large-table strategy**: documented + bounds-tested that Workbench's `PAGE_SIZE` (server-side pagination, not virtualization) is the 40k-SKU strategy — `MAX_PAGE_SIZE = 200` constant + a module-load bounds check + a test proving a full 200-row page renders completely and promptly (no virtualization library added).
- [x] **Error/empty/stale states audit**: found every view already had loading+empty states, but **no view's error state had a working Retry** (text only). Built a shared `<QueryState>` (`QueryLoading`/`QueryError`/`QueryEmpty`) and retrofitted all 7 views (+ Scenarios' 2 secondary error states — solve-mutation and saved-scenarios-list, neither of which had one either) — every error state now has a role=alert + a Retry button wired to the query's `refetch()`/mutation's `mutate()`. Also fixed a genuinely misleading AI-Recommendations empty-state message ("No recommendation detail loaded yet." shown even when the queue was legitimately empty, not still loading) — now distinguishes "You're all caught up" from "still loading detail." `staleTime: 60_000` added to the 4 read-heavy queries (`useDashboard`/`useForecast`/`useFeeds`/`usePartContext`); the interactive `useQueue`/`useRecommendation`/`useKillSwitch` deliberately kept at the default (documented why — staleness there would mask the effects of approve/reject/defer or a kill-switch toggle from another tab). The real query `dataUpdatedAt` (not render-time "now") now feeds every touched view's `ProvChip` freshness stamp.
- [x] **Trackers/docs**: `CLAUDE.md` Section A gains the `apps/web` run/test-table row + a descriptive bullet (7 views, provenance invariant, Docker `:8089`); `apps/web/UAT.md` created from scratch (11 areas, 72 cases, 70 automated); `ROADMAP.md` #7 bullet + this file.
- [x] **e2e (best-effort, landed)**: `npx playwright install chromium` succeeded (this machine already had prior Playwright cache from other work) — added `@playwright/test`, one spec (`e2e/workbench-accept.spec.ts`: mocked BFF via `page.route()` → accept → row leaves the list), `npm run e2e`. Caught + fixed 2 real environment gaps along the way: the default dev port 5173 collided with an unrelated project's already-running Vite server on this shared machine (moved the e2e config to a dedicated port 5190, `reuseExistingServer: false`); Vitest's default file glob was picking up the new `.spec.ts` and trying to run it in the wrong runner (excluded `e2e/**`, preserving Vitest's other default excludes via `defaultExclude` rather than silently dropping them).
- [x] **142 Vitest tests (was 112)**, `npm run build` (tsc -b + vite) clean, `npm run lint` clean (0 errors; the 2 pre-existing `react-refresh` warnings in untouched `ui/badge.tsx`/`ui/button.tsx` are confirmed present on a clean baseline, not introduced by this slice).
- Did **not** touch Docker/`docker-compose.yml` per the slice's explicit instruction — the controller redeploys `:8089` after this lands. Did not modify the BFF.
- Next: e2e coverage for the other 6 views + a full keyboard/screen-reader traversal + automated color-contrast (all flagged as future work in `apps/web/UAT.md` §5); #8 BVR pipeline (Wave 2 exit).

### Completed 2026-07-01 — Part context + portfolio dashboard (BFF), docs/trackers, Docker redeploy
- [x] **BFF** (`services/agent-spine`): `GET /v1/tenants/{tenant}/parts/{pn}/{location}` → `PartContext` (part attributes/description, stock breakdown, lead-time view, open orders, 24-month demand timeseries + summary); `GET /v1/tenants/{tenant}/dashboard` → `DashboardSummary` (portfolio KPIs — parts, total on-hand + value, total shortage, projected demand, AOG exposure, open recommendations, net cost impact — plus by-criticality/by-ATA/by-part-class/by-tier breakdowns and top-shortages). `PlannerStore.from_extract` now retains the in-memory feature store + full `(pn,location)` keys universe to serve these reads; degrades gracefully via a `_safe()` wrapper.
- [x] **Real counts verified by running the suite**: `cd services/agent-spine && uv run --extra bff pytest` → **136 tests** (was 112).
- [x] **Docker redeploy** — `docker compose build bff ui` (single sequential build) then `docker compose up -d`; verified `GET /v1/tenants/acme/dashboard` returns a positive `parts` count and `GET /` returns 200. Project-scoped to `trax-io-planner`; oracle19c/MySQL untouched.
- Next: richer color-contrast/AAA audit, other pre-existing deferred follow-ups; #8 BVR pipeline (Wave 2 exit).

## Prior Session — 2026-06-28

### In Progress
- **Local full-stack Docker deploy is live** for hands-on testing: `docker compose up --build` (repo root, project `trax-io-planner`) runs the **BFF** (`deploy/bff.Dockerfile` — uv over the path-dep packages, seeded from the sample extract via new `trax_io_spine.bff.asgi:app`). **Open http://localhost:8080**; BFF debug `:8001`. Verified: proxy returns 4 pending recs, BFF healthy. Host 8000 was taken → BFF debug port moved to 8001. Project-scoped; never touches oracle19c/MySQL.
- Nothing else open — running a complete repo-wide test pass (UTA). Next after UTA: **#8 BVR pipeline** (Wave 2 exit), forecasting slice D / Chronos challenger.

### Completed 2026-06-28 — Writeback #6 hardening (provenance · rollback · shadow)
- [x] **Brainstorm (4-reader grounding workflow) → spec → plan → build**: [design](docs/superpowers/specs/2026-06-28-writeback-hardening-design.md) → [plan](docs/superpowers/plans/2026-06-28-writeback-hardening.md) (6 TDD tasks) → [ADR-0010](docs/adr/2026-06-28-0010-audited-writeback-seam.md). Grounding mapped exactly what existed (thin seam, success-only `.history`) vs the contract (PN_INVENTORY_LEVEL_HISTORY schema, 90-day rollback, shadow) and the legacy Java writeback columns.
- [x] Built the hardening via a **deterministic SDD workflow** (6 tasks, per-task implement → adversarial review → fix-loop, then opus whole-branch review = **Approve, zero must-fix**; Task 5 went through one fix loop). `AuditedWritebackTarget` Protocol; per-key `HistoryEntry` provenance ledger; rollback (non-zero window, chained entry, first-write→NOTHING_TO_REVERT); shadow-mode (`SHADOWED` + `run --shadow`). `fake_emro` **backed by** `InMemoryWritebackTarget` (no mock drift); `RestWritebackClient` parity over httpx ASGI. **88 tests, ruff clean; live `run --shadow` → `{written:0, shadowed:2, queued:2, rejected:2}`.**
- **Implementer divergence (kept + documented in ADR-0010):** shadow mode also shadow-logs would-be-**queued** writes (any rec with a writable policy), not just would-be-approved ones — more design-faithful (the Evaluation Pipeline wants every intended write logged). Opus approved with the doc caveat.
- ⚠️ iCloud `.venv` corruption struck again (the `_editable_impl_…spine 2.pth` duplicate broke the CLI import + test collection until a clean `rm -rf .venv && uv sync`). [[icloud-sync-conflicts]] — still recommend de-syncing this repo.

### Completed 2026-06-28 — Planner-UI BFF (backend-for-frontend; surviving backend, powers `apps/web`)
- [x] **Grounding (4-reader) → spec → plan → build**: [design](docs/superpowers/specs/2026-06-28-planner-ui-bff-design.md) → [plan](docs/superpowers/plans/2026-06-28-planner-ui-bff.md) (5 TDD tasks) → [ADR-0011](docs/adr/2026-06-28-0011-planner-ui-bff.md). **Scope fork (user chose BFF-first):** the design split the original #7 slice into a React-frontend-in-eMRO + a Trax FastAPI BFF; building the BFF first defines the OpenAPI contract and keeps the slice in the proven Python pattern before the Node-toolchain pivot. Probed the sample first (4 queued: 2 with policy / 2 non_policy) so edge tests hit real data.
- [x] Built `agent-spine/bff/` (`models.py` + `store.py` + `app.py`, `--extra bff`) via a **deterministic SDD workflow** (5 tasks, per-task implement → adversarial review, then opus final review = **Approve**). `PlannerStore.from_extract` reuses the real pipeline and keeps the `(rec, outcome)` pairs the Supervisor discards; lifecycle PENDING/APPROVED/REJECTED/DEFERRED; approve→writeback (#6 audited seam); bulk-approve/history/rollback/kill-switch. FastAPI app maps store exceptions to 404/409/422/423. **24 BFF + 112 agent-spine tests, ruff clean; live `queue→approve→history` verified** (4 pending → approve → v1 written → 3 pending). This BFF is the backend now powering `apps/web` — it outlived the original React frontend it was designed alongside.
- [x] **Must-fix applied:** added the spec-§4 tenant-isolation test (two tenants, A can't see/act on B's recs) — a CLAUDE.md 4-layer-isolation rule. Grounding had over-claimed a nested `DemandProjection` on the recommendation; the plan/build correctly used the real exposed fields (`projected_demand` + policy/current_policy/evidence).
- ⚠️ iCloud struck a 4th time (13 `· 2.py`/`.pyc` duplicates inflated the build's reported test count 112→132 by re-collecting writeback tests; broke repo-level ruff with N999). Cleaned; true count 112. [[icloud-sync-conflicts]] — **de-sync this repo.**

### Completed 2026-06-28 — Forecasting #5 slice C (empirical-Bayes ultra_rare)
- [x] **Brainstorm (5-reader grounding workflow) → spec → plan → build**: [design](docs/superpowers/specs/2026-06-28-forecasting-empirical-bayes-ultra-rare-design.md) → plan (7 TDD tasks) → [ADR-0013](docs/adr/2026-06-28-0013-empirical-bayes-ultra-rare-projector.md). **Backend choice (probed):** pure `numpy`/`scipy` (pre-existing), no `torch`/SageMaker/heavy-dep — Chronos/Moirai challenger deferred (same libomp-class constraint as LightGBM in slice B). `fit_prior` MoM closed-form confirmed deterministic cross-process.
- [x] Built `eb.py` (`GammaPrior`, `fit_prior` MoM + overdispersion guard, `posterior_rate`, `posterior_predictive_var`; non-finite guards), `peer_priors.py` (`PeerRecord`, `peer_record_from_context`, `PeerPriorProvider.fit/get_prior` with L0→L3 coarsening backoff; `min_peers` default 5), `eb_projector.py` (`EmpiricalBayesProjector` handles `ULTRA_RARE`, delegates other regimes + missing attrs to fallback, wrapped try/except → never raises; `build_eb_projector` batch pre-pass), `backtest.py` (`eb_rate_fn` MASE hook) via a **deterministic SDD workflow** (7 TDD tasks, per-task implement → adversarial review). **61 forecasting tests, ruff clean.**
- [x] **Key design decision (recorded in ADR-0013):** `peer_record_from_context` sets `exposure = 0.0` for a brand-new PN (empty observations list). Its posterior collapses to `α/β` — exactly the peer-group mean — fulfilling design §5.1 *"new PN … inherit the peer-group rate."* An established-but-rare part with non-empty observations retains full `basis_window_days` exposure and is genuinely shrunk below the peer mean. `PeerPriorProvider.fit` excludes zero-exposure records to avoid 0/0 in prior fitting.
- [x] `EmpiricalBayesProjector` wires into `RecommendationService(projector=…)` via the existing kwarg — #11's 142 tests unchanged; the policy engine sees a structurally identical `COMPOUND_POISSON` projection (only `lambda`'s source and widened `std_per_day` change).
- Deferrals: Chronos/Moirai zero-shot challenger; cross-tenant federated peer priors (design §5.3); `clump_p` estimation (stays 1.0); regime hysteresis; Supervisor two-pass wiring.

### Completed 2026-06-28 — Forecasting #5 slice B (gradient-boosted moderate/high)
- [x] **Brainstorm (5-reader grounding workflow) → spec → plan → build**: [design](docs/superpowers/specs/2026-06-27-forecasting-gradient-boosted-design.md) → [plan](docs/superpowers/plans/2026-06-27-forecasting-gradient-boosted.md) (4 TDD tasks) → [ADR-0009](docs/adr/2026-06-27-0009-gradient-boosted-projector.md). **Backend fork (probed):** LightGBM's wheel installs on py3.14 but needs a system `libomp.dylib` (not self-contained) — so chose sklearn `HistGradientBoostingRegressor` (same model family, zero system dep, verified). User picked it. Also de-risked all the tricky numeric assertions (HGB determinism, ramp/cold-start/std-floor) with an inline probe before the build.
- [x] Built `GradientBoostedProjector` (`features.py` + `gradient_boosted.py` + `gb_projector.py`) via a **deterministic SDD workflow** (per-task implement → adversarial review → fix-loop, then opus whole-branch review = **Approve, zero must-fix, no fix loops**). Per-key autoregressive (lag+rolling) HGB → next-period mean + residual variance → NORMAL `DemandProjection` mirroring the deterministic projector field-for-field (only the mean's source changes); non-target regimes + cold-start delegate. `gb_next_rate` reuses the MASE backtest unchanged; #11 integration via the existing `projector=` kwarg. **34 tests, ruff clean.** Opus verified cross-process determinism (identical float bits) + the NORMAL mirror line-by-line.
- **Honesty call (in spec + ADR):** the design's *causal-covariate* LightGBM is aspirational — `causal_utilization` is stubbed in v1 and static covariates are constant per-key — so slice B ships the genuinely-trainable per-key AR core; causal/global/SageMaker/Chronos/45-day-gate deferred.

### Completed 2026-06-27 — End-to-end event ingestion (agent-spine EventIngestor + ingest CLI)
- [x] **Brainstorm → spec → plan → build**: [design](docs/superpowers/specs/2026-06-27-event-ingestion-end-to-end-design.md) → [plan](docs/superpowers/plans/2026-06-27-event-ingestion-end-to-end.md) (5 TDD tasks) → [ADR-0008](docs/adr/2026-06-27-0008-consumer-side-event-ingestor.md). Grounding caught the keystone: ingestion **must** live in agent-spine (imports the canonical schema), not the event-publisher endpoint — else the one-way dep inverts into a cycle. Probed the real engine first to fix the PROCESSED/NO_OP classification (`recommendations>0`).
- [x] Built `event_lane/ingestor.py` (`EventIngestor` + `IngestStatus`/`IngestOutcome`/`IngestReport` + `DeadLetterSink`) + `trax-io-spine ingest` CLI via a **deterministic SDD workflow** (per-task implement → adversarial review → fix-loop, then opus whole-branch review = **Approve, zero must-fix, no fix loops needed**). Dedup by `event_id`, adapt via `canonical_adapter`, recompute via the unchanged `EventLaneHandler`; malformed → dead-lettered, never raises. Integration test feeds #3's `make_event` oracle through it end to end. **62 tests (14 new), ruff clean; live `trax-io-spine ingest` verified** (4 events → 1 processed/1 dup/1 no-op/1 invalid).
- ⚠️ **Environment:** the `Documents/Claude/Projects/` folder is iCloud-synced and kept creating conflict-copy duplicates (` 2.py`/` 2.pth`/` 3.lock`) — both in the repo (cleaned; untracked, never reached `main`) and inside `.venv` (broke the editable install of the spine CLI until a clean `rm -rf .venv && uv sync`). **Recommend excluding this repo from iCloud sync.**

### Completed 2026-06-27 — Event Publisher #3 slice A (canonical wire-contract + fake_emro harness)
- [x] **Brainstorm (6-reader grounding workflow) → spec → plan → build**: [design](docs/superpowers/specs/2026-06-27-event-publisher-contract-harness-design.md) → [plan](docs/superpowers/plans/2026-06-27-event-publisher-contract-harness.md) (8 TDD tasks + conditional Schemathesis) → [ADR-0007](docs/adr/2026-06-27-0007-event-publisher-canonical-contract-harness.md). Grounding caught the load-bearing tension: the shipped event lane consumes **slim** `DomainEvent`s but the Apr-14 contract is **richer** — two sources of truth. Decision (user-approved): new canonical package as single source of truth + one-way consumer adapter.
- [x] Built `services/event-publisher/` (`trax_io_event_publisher`) via a **deterministic SDD workflow** (per-task implement → adversarial spec+quality review → fix-loop, then opus whole-branch review). Canonical full-fidelity 7-event schema, `EventPublisher` retry/backoff/DLQ behind a `Transport` seam, FastAPI `fake_event_endpoint`, `AsgiTransport` round-trip, `event_lane/canonical_adapter` (slim models untouched). **64 event-publisher tests + agent-spine 48 (+2 handler-hop), ruff clean.**
- [x] Opus final review = **Fix-then-merge**; fix pass landed: `transaction_no str→int` (the harness had drifted from its own contract — int `88412`) + `test_contract_examples.py` parsing all 7 contract examples verbatim; regenerated both uv.lock files (`uv sync --locked` was failing); dropped the Schemathesis extra (breaks `http` on py3.14) and fixed a latent 400-handler serialization bug it had papered over.
- Lesson: the SDD workflow's *conditional* Schemathesis branch skipped its fix loop — caught in the controller's final-review pass. Conditional workflow branches need the same fix-loop wrapper as the main path.

### Completed 2026-06-27 — Forecasting #5 slice A (classical intermittent)
- [x] **Brainstorm → spec → plan → build**: [design](docs/superpowers/specs/2026-06-27-forecasting-classical-intermittent-design.md) → [plan](docs/superpowers/plans/2026-06-27-forecasting-classical-intermittent.md) (6 TDD tasks) → [ADR-0006](docs/adr/2026-06-27-0006-statistical-projector-behind-demandprojector.md). Decomposed #5 (policy engine = #11's mini_engine; net-new = forecasting); grounded that statsforecast runs on **py3.14 with scipy>=1.18 + numba>=0.62 pinned**, and that the seam is #11's `DemandProjector` Protocol.
- [x] Built `services/forecasting/` (`StatisticalProjector` + statsforecast Croston/SBA/TSB + SBC selection + MASE backtest) via subagent-driven TDD (per-task + opus final review, **Fix-then-merge → fixed**). The fitted intermittent `DemandProjection` **mirrors #11's intermittent branch field-for-field** (only λ's source changes: fitted vs average — opus-verified), so the policy engine treats it identically with a better λ. One backward-compatible #11 change made the projector injectable. **19 forecasting tests + #11's 142 unchanged, ruff clean.**
- The implementers caught + fixed several plan-snippet bugs (DemandHistory needs extract_date; a zero-gap-fill assertion; the SBC ADI=2.0 boundary; a constant-series MASE) — good adversarial implementation.
- Forecasting follow-ups: compound-clump (`clump_p`) estimation; slices B/C/D.

### Completed 2026-06-27 — Event lane (#4 + #2)
- [x] **Brainstorm → spec → plan → build** for the event lane: [design](docs/superpowers/specs/2026-06-27-event-lane-design.md) → [plan](docs/superpowers/plans/2026-06-27-event-lane.md) (5 TDD tasks). Closes the gap where #2's DynamoDB online `FeatureBundle` layer had no consumer.
- [x] Built `event_lane/` (events, `DirectKeyResolver`, `BundleFeatureStore`/`BundleInventoryState` adapters, `OnlineStore` + `InMemoryOnlineStore`, `EventLaneHandler`) via subagent-driven TDD (per-task + opus final review, **Ready to merge, no must-fix**). The `BundleFeatureStore` adapter lets the **whole Supervisor + #11 engine run unchanged against the online bundle** — the end-to-end test proves it (`skipped==0` over a materialized bundle => the real engine read every required group from the bundle). **64 tests green, ruff clean.**
- Tracked follow-ups: fan-out key resolution (production index); incremental online-bundle update; AWS event transport; `adapters.py` `_check` on the 2 always-raise methods.

### Completed 2026-06-27 — Cedar authorization (#4)
- [x] **Brainstorm → spec → plan → build** for the Cedar autonomy slice: [design](docs/superpowers/specs/2026-06-27-cedar-authorization-design.md) → [plan](docs/superpowers/plans/2026-06-27-cedar-authorization.md) (3 TDD tasks). The brainstorm *grounded* the design by verifying `cedarpy` runs in-process and that **Cedar has no float type** → delta crosses as integer basis points.
- [x] Built `CedarAutonomyPolicy` + `autonomy_bands.cedar` (design §6.1 bands) behind the existing `AutonomyPolicy` Protocol, via subagent-driven TDD (per-task + opus final review, **Ready to merge, no must-fix**). `CedarAuthorizer` is fail-safe — a parse/eval error (`Decision.NoDecision`) raises `CedarPolicyError`, never a silent allow (verified empirically). cedarpy is opt-in behind a `cedar` extra (lazy-imported; default stays `BandAutonomyPolicy`). **49 tests green, ruff clean.**
- Tracked follow-ups: align `BandAutonomyPolicy` defaults to §6.1; Cedar `schema=` validation; the `cedar.py` return-line `# noqa` cleanup.

### Completed 2026-06-27 — Agent Spine v1 (deterministic orchestration core)
- [x] **Brainstorm → spec → plan → build** for #4, adapting the 2026-04-14 AgentCore plan to today's reality (real #2/#11, monorepo): [design](docs/superpowers/specs/2026-06-27-agent-spine-v1-design.md) → [plan](docs/superpowers/plans/2026-06-27-agent-spine-v1.md) (12 TDD tasks) → [ADR-0005](docs/adr/2026-06-27-0005-deterministic-agent-spine-core.md).
- [x] Built `services/agent-spine/` (`trax_io_spine`) via **subagent-driven TDD** (fresh implementer + 2-verdict reviewer per task, opus final whole-branch review + one fix pass): contracts (promote #11 mirrors), `tenant_scope` contextvar, guardrail (hard §6.2 verify + `BandAutonomyPolicy` + `GuardrailEnforcer`), writeback (`InMemoryWritebackTarget` + `fake_emro` FastAPI + httpx `RestWritebackClient`), `Supervisor` orchestration, `trax-io-spine` CLI, end-to-end + tenant-isolation integration tests. **34 tests green, ruff clean.**
- [x] **Milestone #8** reached offline: `trax-io-spine run --extract-dir … --tenant acme` → `{recommendations:6, queued:4, rejected:2, written:0}` against the REAL #11 engine.
- [x] Final-review fix pass: writeback idempotency key → content-addressed `input_snapshot_hash` (re-running the same extract dedups, not run-date); dead-code/hardening; README.
- Tracked follow-ups (non-blocking): `_PolicyLike` Protocol for the CurrentPolicy seam; `RestWritebackClient` `aclose()` + FAILED-path test; CLI `--apply`-without-URL guard.

## Prior Session — 2026-06-26

### Completed This Session
- [x] Ran `/init-project`: scaffolded CLAUDE.md, ROADMAP.md, TASKS.md, and `.claude/` workspace (`skills/`, `agents/`, `memory/lessons.md`)
- [x] Mapped roadmap from [docs/roadmap/2026-04-14-trax-io-v1-build-roadmap.md](docs/roadmap/2026-04-14-trax-io-v1-build-roadmap.md) into ROADMAP.md
- [x] Anchored CLAUDE.md to the authoritative design / roadmap / plans layout
- [x] Sub-project #1 Phase 1 scaffold: `tools/nightly-extract/` (Click CLI skeleton, 12 SQL placeholder files, `uv` + `pytest` + `ruff`, README + smoke test)
- [x] Sub-project #9 Phase 1 scaffold: `infra/observability-soc2/` (CDK Python stack — CloudTrail Lake 7-yr, Audit Manager SOC 2 assessment, per-tenant KMS CMKs w/ annual rotation, S3 Object Lock Compliance 7-yr, OTel collector Fargate placeholder, per-tenant log groups; `uv` + `pytest` synth tests + `ruff`; `README.md` + `docs/soc2-onboarding.md`)
- [x] Sub-project #2 Phase 1 scaffold: `services/feature-store/` (FeatureStoreClient Protocol w/ `TenantContext`, `InMemoryFeatureStore` stub per ADR-0002, 10 pydantic feature-group schemas, `uv`+`pytest`+`ruff`) + `infra/feature-store/` (CDK stack: per-tenant KMS, S3 landing/lake, Glue DB, DynamoDB online layer, 10 Iceberg column schemas, synth tests)
- [x] Saved project memory: AWS account name is `TraxAi` (not to be confused with product name "Trax IO")
- [x] Verified Phase 1 scaffolds: all 4 test suites green (32 tests total — 3 + 16 + 8 + 5) — 2026-04-16
- [x] Fixed `infra/observability-soc2` CDK bug: `tags_raw=` → `tags=` with `CfnTag` objects on `CfnEventDataStore`
- [x] Drafted [ExtractManifest contract](docs/contracts/2026-04-17-extract-manifest-contract.md) — 21 canonical domains, bind-var map, atomicity rules, manifest v1.0.0 schema — 2026-04-17
- [x] Rewrote `tools/nightly-extract/` for 21 real domains (from `~/Downloads/PTC Project Files/eMRO Data SQLs.sql`): bind-var parameterized SQLs, `ExtractManifest` pydantic model, domain registry, 45 tests green — 2026-04-17
- [x] **Wave 0 Phase 2 slice** (3 parallel subagents, 152 tests green total) — 2026-04-17
  - **#1 nightly-extract** (73 tests): `oracle.py` (oracledb thin driver + context mgr), `binds.py` (contract-driven bind resolver), `runner.py` (per-domain atomicity + manifest emission + source_sql_sha256), CLI `--dry-run/--no-dry-run`, ULID run_ids
  - **#2 feature-store** (28+11 tests): PySpark `demand_history` Glue job (manifest-driven, Iceberg writer, ANSI-safe casts, `unionByName(allowMissingColumns=True)`, lazy `awsglue` import), CDK Glue job packaging w/ tenant-scoped IAM role (Glue 4.0, G.1X × 2)
  - **#9 observability-soc2** (40 tests): multi-tenant refactor — `TenantSpec` frozen dataclass + `LIGHTHOUSE_TENANTS` + `load_tenants_from_env()`, per-tenant KMS/log-group loop, `CfnOutput` contract (`TraxIo-<tenant>-TenantKmsArn`, `TraxIo-<tenant>-TenantLogGroupArn`), `iam_helpers.tenant_tag_condition()` + `apply_tenant_tags()`
- [x] **Sub-project #11 — Recommendation Engine (deterministic v1)** brainstormed → spec'd → planned → **built end-to-end** (`services/recommendation-engine/`, 123 tests green, ruff clean) — 2026-04-17
  - Flow: `/brainstorming` → [spec](docs/superpowers/specs/2026-04-17-trax-io-recommendation-engine-design.md) (recon + 4-lens adversarial review, all approve-with-fixes, every finding folded in) → [plan](docs/superpowers/plans/2026-04-17-trax-io-recommendation-engine.md) → [ADR-0004](docs/adr/2026-04-17-0004-deterministic-recommendation-layer.md) + roadmap amendment (register 10→11)
  - Engine: net-position core; deterministic regime classifier; demand projector (per-day rate + compound-Poisson/NBD params); policy engine ((S−1,S)/(s,S)/(R,Q) + numeric quantile path + §6.2 constraints); 5 recommenders (Adjust/Purchase/Transfer/Reduce/Sell); arbitration (transfer-before-purchase, no contradictions); AOG risk scorer (part-class recovery time); confidence + deterministic ranking; content-addressed provenance ids
  - Surfaces: library facade, `trax-io-reco` click CLI, optional FastAPI read API (behind `api` extra)
  - Forward-compatible contract mirrors (`Regime`/`CanonicalCriticality`/`PolicyKind`/`AutonomyTier`/`ForecastHorizon`/`PolicyRecommendation`) for promotion to Agent Spine #4
  - **4-lens adversarial code review** (correctness / spec-compliance / determinism / test-adequacy) after build — found 1 critical + several major bugs, **all fixed + locked with 12 regression tests** (135 tests total): interchange both-short over-buy (rep-member fix), one-way-interchange collapse, `apportion()` zero-consumption over-allocation, bucket-blind variance scaling, Decimal-scale hash non-invariance, order-dependent vendor resolution, banker-rounding flips (`round_half_up`), negative-unit_cost guard, zero-qty residual purchase, ranking criticality weighting
  - Lesson captured: uv editable path deps (cross-project AND the workspace's own console script) don't reliably expose src-layout packages → non-editable path source / `--reinstall-package` + `-m`
- [x] **#1 + #2 Phase 2 — data pipeline (full slice, recon via 3-agent workflow → build)** — 2026-04-17. Unlocks a shadow-mode dry run: real eMRO extract → judge-able recommendation batch with no AWS/Oracle/Spark.
  - **#1 nightly-extract:** fixed the `stock_level_upload` #19 PN/LOCATION alias transposition at the SQL source; added `pythonpath=["src"]` to pytest config (durable fix for the editable-venv collection flakiness) — 73 tests green
  - **#2 feature-store:** promoted `StockPosition` (`stock_amount` #18) + `CurrentPolicy` (`stock_level_upload` #19) into real FS feature groups — schemas + `FeatureStoreClient.get_stock_position`/`get_current_policy` + InMemory buckets + Iceberg column maps (31 tests green)
  - **engine refactor:** stock/policy now read from the FeatureStoreClient (FeatureReader maps FS→lean context types); `InventoryStateProvider` shrank to the 3 genuine stubs (scheduled_demand, aog_signal, repair_tat)
  - **bridge (the unlock):** `data/extract_loader.build_stores_from_extract` — reads 21 `<domain>.json` + manifest, applies real transforms (stock/policy/attrs/vendor column-maps, monthly demand bucketing, lead-time from closed-order dates, open-orders, interchange graph), seeds the engine stores; CLI `--extract-dir`; committed `examples/extract_sample/`; golden test asserts transfer/sell/adjust on real-shaped data (138 engine tests green); verified CLI: 6 recs, 0 skipped off the sample extract dir
  - hardened after adversarial review: non-finite guards, required-domain check, corrupt-JSON tolerance, Oracle `MM/dd/yyyy` date parsing, no phantom expendable demand (142 engine tests)
- [x] **#1 S3 landing writer** (recon-scoped → build → focused review) — 2026-04-17. `LandingSink` abstraction so the extract lands to S3 and the presigned-vs-boto3 fork + KMS are swappable, not blockers.
  - `landing.py`: `LandingSink` Protocol + `landing_prefix()` + `LocalFsSink` (local disk) + `S3Sink` (boto3-style injected client, optional SSE-KMS via per-tenant CMK)
  - `runner.py` refactored to write every artifact + manifest through the sink; populates `DomainArtifact.s3_uri`; manifest landed LAST (a crashed run leaves an incomplete, manifest-less prefix that #2 Glue ignores)
  - CLI `--landing s3://bucket[/prefix]` + `--kms-key-id`; `boto3` lazy-imported (new `s3` extra) so local path + tests need no AWS; full S3 path tested via mocked boto3 (81 extract tests green, ruff clean)
  - presigned-URL (credential-less, customer-run) sink is a documented future `LandingSink` impl
- [x] **#2 Glue loaders — stock_position + current_policy** — 2026-04-17. Materialize the two promoted feature groups into Iceberg, following the `demand_history_job` template.
  - `glue/_common.py` (shared `load_manifest` / `select_artifacts(domains)` / `read_artifacts` / `append_iceberg`); `glue/stock_position_job.py` (stock_amount #18 → on_hand/serviceable/in_repair/allocated/rental/loan, deduped) + `glue/current_policy_job.py` (stock_level_upload #19 → rop/eoq/safety_stock/max_stock/replenishment_lead_days)
  - transforms **verified on a real local SparkSession** (JDK present): dedup on (pn, location), null-pn drop, integer/double casts, positional column order, case-insensitive alias resolution (35 feature-store tests)
  - CDK stack generalized: `_make_glue_job(feature_group=…)` packages all 3 jobs (per-tenant least-priv role + `CfnJob`); synth asserts 3 jobs (11 infra tests)
- [x] **#2 Glue loaders — vendor_economics + part_attributes + criticality** — 2026-06-26. Completes Iceberg materialization for *every required engine input* (demand/stock/policy already shipped).
  - `glue/vendor_economics_job.py`: joins `pn_vendor_price` #16 with `part_master` #15 costs; keeps one row per (pn, actual vendor) **and** synthesizes a `vendor="DEFAULT"` canonical row via a Window (preferred → cheapest → vendor-id) so the assembler resolves both its open-order-vendor path and its no-open-order fallback; MinOQ floored to 1; `kit_cost` null (legacy `getKitCost` not ported); tolerant of missing `part_master` (costs → null)
  - `glue/part_attributes_job.py`: `part_master` → part_class precedence (ispartkit→rotable, else serializable/repairable→repairable, else expendable), hazmat/tool boolean coercion (null-safe), non-positive shelf-life/tail-count → null — mirrors the reco bridge `_part_class`/`_truthy`/`_i(..) or None`
  - `glue/criticality_job.py`: `part_master.HostPartCriticalID` → raw code (original case preserved) + canonical 1–5 tier via the shared default essentiality map (blank/unknown → tier 4); `mapping_source="auto_inferred"`
  - all three transforms **verified on a real local SparkSession** (preferred-vendor DEFAULT selection, cost left-join no fan-out, dedup, null-drop, tier mapping, column order) — 7 new Spark tests (42 feature-store tests total)
  - CDK `_GLUE_FEATURE_GROUPS` now packages **6 jobs**; synth asserts 6 (11 infra tests)
  - **post-build adversarial review → cast-fidelity hardening across ALL 6 jobs** (the review caught it on the 3 new ones; the latent bug was identical in the 3 committed ones, so fixed uniformly):
    - the real extract delivers every numeric as a **string**, and local Spark 4.x runs ANSI **on** (bad cast throws) while prod Glue 4.0 runs ANSI **off** (bad cast → null) — so tests didn't match prod, and a bare `cast(int)` **truncates** `"365.5"`→365 whereas the bridge `_i` **rounds**→366
    - shared `glue/_common`: `coerce_int(col, default)` (= `coalesce(bround(double).cast(int), default)`, HALF_EVEN to match Python `round`) + `disable_ansi_mode(spark)`; every job routes integer coercions through `coerce_int` and pins ANSI off in `main()`; conftest pins ANSI off too
    - `demand_history` deeper fix: round `HistoryAmount` **per row** inside the `sum` (round-then-sum, not sum-then-truncate) — two `"2.5"` issues now correctly give 4
    - all 6 test suites restrung to **string-typed** numerics + a fractional regression each; `coerce_int` proven **exactly equivalent to the bridge `_i`** across a 23-input edge battery on real Spark; `vendor_economics` guarded against a reserved-`DEFAULT`-vendor collision (47 feature-store tests, ruff clean)
    - lesson captured in `.claude/memory/lessons.md` (string extract + ANSI mode + cast rounding)
- [x] **#2 Glue loaders — derived/graph groups (lead_time + open_orders + interchange + location_graph)** — 2026-06-26. **Every v1 feature group the engine reads now materializes into Iceberg** (only the unused-in-v1 `causal_utilization`/`wash_rate` remain).
  - `lead_time_distribution_job`: `pn_vendor_price` (preferred-vendor promised lead, 0/missing→21) joined with `order_plan_closed_orders` (realized lead days = ActualRcvDate−PlanOrderDate ≥ 0); replicates the bridge's **index-based** percentiles (`realized[n//2]`, `realized[min(n−1, round(0.9·(n−1)))]`) via `element_at`+`bround` — NOT `percentile_approx`; promised-only fallback (mean=p50=promised, p90=×1.3, p99=×1.6, n=0) when no realized history
  - `open_orders_snapshot_job`: `order_plan` OPEN → per-(pn,location) snapshot with sorted `array<struct<order_id,order_type,vendor,qty_open,expected_rcv_date>>` + `total_open_qty`; qty_open floored at 0 and >0-filtered
  - `interchangeable_graph_job`: `part_chain_details` → explode each edge to both endpoints, per-PN distinct sorted `members` + sorted `edges` array<struct>, `group_id = "+".join(sorted(members))`, `one_way` from RelationType==1
  - `location_graph_job`: `location_master` → role (main/outstation) + parent; `children` empty to match the bridge
  - shared `glue/_common.parse_extract_date` added (Oracle `MM/dd/yyyy[ HH:mm]` + ISO, via `try_to_timestamp`); all reuse `coerce_int`/`disable_ansi_mode`
  - 9 new Spark tests with **bridge-formula expected values** (lead_time mean/p50/p90/p99 hand-computed; interchange members/edges/group_id; open_orders sort+total; location role edge-cases) — 56 feature-store tests, ruff clean
  - CDK `_GLUE_FEATURE_GROUPS` packages **10 jobs**; synth asserts 10 (11 infra tests)
  - post-build adversarial correctness/fidelity review run on the 4 transforms
- [x] **#2 Production read client — `GlueIcebergFeatureStore` (Phase 6)** — 2026-06-26. Closes the production loop: extract → Glue → Iceberg → read client → engine, conforming to the same `FeatureStoreClient` Protocol (ADR-0002 one-line DI swap).
  - `iceberg_store.py`: pyiceberg-based (pure Python, no Spark/JVM); catalog **injected** (GlueCatalog in prod, local SQLite `SqlCatalog` in tests). Each method requires a `TenantContext` (`_require_tenant`), scans `{namespace}.{group}` filtered on the `tenant_id` partition + key, resolves the **latest `extract_date`**, maps the row → the same pydantic model the in-memory stub returns
  - mapping: generic field-projection for the 6 flat groups; custom for `demand_history` (exploded rows → sorted `observations`, `source` default applied since Glue writes hyphenated "nightly-extract"), `open_orders`/`interchange` (nested `array<struct>`), `location_graph` (flat → nested `LocationNode`); missing table (causal/wash, unmaterialized) → `FeatureStoreLookupError`, not a crash
  - new `iceberg` extra (`pyiceberg[sql-sqlite]`, `pyarrow`); de-risked first with a local-catalog round-trip spike
  - tests (`tests/iceberg/`): per-method/edge (latest-snapshot, decimal/null, demand+wash aggregation, nested, missing/empty table, missing tenant, cross-tenant, re-append last-write-wins) + **the ADR-0002 shared contract test** (12 parametrized equivalence cases covering every method + identical-error tenant isolation) — proves in-memory ≡ Iceberg and lossless round-trip
  - **adversarial review folded in**: (1) `get_wash_rate_history` was returning empty `points` — wash_rate is *exploded* in the lake like demand_history, so it now aggregates (latent data-loss bug fixed before any wash job ships); (2) `_scan_latest` now resolves the latest `ingested_at` within the latest `extract_date` so a re-appended partition is **last-write-wins** (deterministic, matching the stub) since Iceberg appends don't dedupe; (3) corrected the "no table" premise (CDK creates all 12 tables; causal/wash are *empty* tables → empty-rows lookup error) + added empty-table coverage; (4) extended the contract test to all 12 methods (added wash + causal)
  - 87 feature-store tests (27 new; skip cleanly without the `iceberg` extra), ruff clean; README/CLAUDE.md/ROADMAP updated
- [x] **#2 Online-feature layer — `DynamoDbOnlineStore` (design §4.2)** — 2026-06-26. The low-latency event-triggered read path; complements the offline Iceberg client.
  - design fork resolved by §4.2 + the CDK (`online_table` SK = `pn_location`): the online layer is a **denormalized per-`(pn,location)` bundle** (one sub-10ms `get_item`), NOT a per-feature-group Protocol store — so no infra change needed
  - `FeatureBundle` schema: the (pn,location)-scoped feature set (stock/policy/demand/open_orders/location/attrs/criticality/interchange) + `vendor_economics`/`lead_time` as small vendor-keyed maps, so the engine resolves a vendor without leaving the bundle; optional members `None` when absent
  - `online_store.DynamoDbOnlineStore`: `put_bundle`/`get_bundle` over an injected boto3 `Table`; PK=`tenant_id`, SK=`f"{pn}#{location}"` (matches CDK); body = bundle JSON; reads require `TenantContext`, cross-tenant structurally misses → `FeatureStoreLookupError`
  - `materialize.materialize_bundle(offline, …)`: pure assembly core (nightly-Glue / event-lane population) over any `FeatureStoreClient`; pulls DEFAULT + open-order vendors; `_opt` swallows only `FeatureStoreLookupError`
  - new `dynamodb` extra (boto3) + `moto[dynamodb]` (dev); moto-backed tests (lossless round-trip incl. Decimal/nested/vendor-map, missing-key, missing-tenant, cross-tenant, upsert, materialize-packs-all incl. open-order vendor, absent-group tolerance, offline→online round-trip)
  - **adversarial review folded in**: (1) CRITICAL — `f"{pn}#{location}"` sort key was non-injective (eMRO PNs/locations can contain `#`; `("A#B","C")` collided with `("A","B#C")` → silent cross-key overwrite/wrong read) → now length-prefixed injective + a `get_bundle` body-matches-request assertion + collision regression test; (2) HIGH — unbounded `demand_history` could exceed DynamoDB's 400 KB item cap → `materialize_bundle` windows observations (default 24; full history stays in Iceberg) + windowing test + 400 KB note in `put_bundle`; (3) HIGH — documented the null-required-field contract (None = absent → engine fails closed, not zero); (4) `FeatureBundle` validator rejects empty pn/location
  - 97 feature-store tests (10 new; skip cleanly without the `dynamodb` extra), ruff clean; README/CLAUDE.md/ROADMAP updated
- [x] **#2 Online-table population logic — `populate_online` + key enumeration** — 2026-06-26. The writer side of the online layer (core of the nightly Glue job + event lane).
  - `online_writer.populate_online(offline, online, *, tenant, keys, …)`: materializes each `(pn, location)` and upserts it; **skips incomplete keys** (required group, default `stock_position`, absent → fail closed rather than write a null-stock bundle indistinguishable from zero — implements the review's HIGH-2 contract); **meters** oversize `put` failures (`ClientError`) instead of silently dropping the busiest parts; returns a `PopulateResult(written/skipped_incomplete/failed_oversize)`
  - `GlueIcebergFeatureStore.iter_inference_keys(tenant)`: distinct tenant-scoped `(pn, location)` from `stock_position` (the inference-key universe), via a `selected_fields` scan; `[]` when the table is absent
  - moto + in-memory tests: write-complete/skip-incomplete + empty-noop + enumeration distinct/tenant-scoped/no-table; **101 feature-store tests** (4 new), ruff clean
  - remaining: CDK Lambda/Glue schedule + event-lane trigger to *invoke* `populate_online` (deploy wiring)

### Blockers / cross-agent contracts
- ~~**#1 ↔ #2 contract:** `ExtractManifest` pydantic model~~ — **RESOLVED 2026-04-17** → [contract](docs/contracts/2026-04-17-extract-manifest-contract.md) + implementation in `tools/nightly-extract/src/trax_io_extract/manifest.py`. 21-domain list is now canonical (matches customer's `eMRO Data SQLs.sql`).
- ~~**Oracle package dependencies:**~~ **RESOLVED 2026-04-17** — product decision: inline the PL/SQL package logic into the extract SQLs so we have zero dependency on `PKG_TRAX_PTC` or `pkg_settings_pn_master`. Reference package body kept at `~/Downloads/PTC Project Files/PKG_TRAX_PTC.sql`. Inlined functions: `getKitCost` + `getRecordsType` (in `15_part_master.sql`); `getPNCategory` (in `03_demand_history_expendables.sql` as direct `pn_master.category` lookup).
- **All sub-projects ↔ #9:** new contract published at [infra/observability-soc2/docs/soc2-onboarding.md](infra/observability-soc2/docs/soc2-onboarding.md) — mandatory reading before any other sub-project's first PR. Defines tagging, per-tenant KMS envelope, audit-log schema, OTel span attrs, PII redaction, IAM condition keys, audit mirror.
- **Cedar policy templates** (design §3.2) not yet drafted — will block tenant-scoped IAM in Phase 2.
- **Bucket naming convention** (`trax-io-<tenant>-<env>-landing/lake`) — decide before first #2 deploy; CDK currently auto-names.
- **ADR-0002 alignment:** canonical `InMemoryFeatureStore` lives in Agent Spine repo (not yet scaffolded). Reference stub shipped in `services/feature-store/` to unblock; move Protocol to Spine when Spine lands.

### Next Session
- **#11 follow-ups:** promote the on-hand-stock / current-policy / scheduled-demand / AOG / repair-TAT stubs into Feature Store #2 read methods; extend interchange rollup to full-network apportionment + one-way-directed transfer donors; fix the `stock_level_upload` #19 PN/LOCATION alias transposition in the extract SQL
- **#1 Phase 2 finish:** S3 landing writer (parquet per domain + manifest upload), real Oracle smoke test against customer staging, begin 14-day clean-run watch
- **#2 Phase 2 expand:** extend Glue job pattern from `demand_history` to the remaining 20 domains; finalize Iceberg schemas; start 24-month backfill for lighthouse
- **#9 Phase 2 finish:** deploy CloudTrail Lake + Audit Manager to TraxAi account; provision per-tenant KMS w/ annual rotation; wire OTel → X-Ray for first real agent hop
- **Cross-cutting:** draft Cedar policy templates (design §3.2) — blocker for tenant-scoped IAM
- **Decision needed:** bucket naming convention (`trax-io-<tenant>-<env>-landing/lake`) before first #2 deploy
- Confirm whether the lighthouse customer is signed (Week 0 milestone in [ROADMAP.md](ROADMAP.md))
