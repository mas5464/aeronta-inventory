{
  "$schema": "https://railway.com/railway.schema.json",
  "build": { "builder": "DOCKERFILE", "dockerfilePath": "deploy/bff.Dockerfile" },
  "deploy": {
    "startCommand": "uv run --no-sync python -m trax_io_spine.pg.worker"
  }
}
